<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vũ Ngọc Phát | BIM Engineer & Construction Manager</title>
    <meta name="description"
        content="Portfolio Vũ Ngọc Phát – Kỹ sư BIM quản lý thi công hạ tầng giao thông, LOD 300-400, 4D/5D, BIM Automation, Tekla, Revit, Civil 3D." />
    <!-- Fonts: Arial system font — no external load needed -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

    <style>
        /* ════════════════════════════════════════════
   DESIGN SYSTEM – BIM TECH ENGINEERING THEME
   ════════════════════════════════════════════ */
        :root {
            /* Palette */
            --bg: #020b14;
            --bg-2: #030f1c;
            --bg-card: rgba(4, 15, 30, 0.82);
            --bg-card-hov: rgba(5, 20, 40, 0.92);
            --primary: #00c2ff;
            /* electric cyan */
            --primary-dim: rgba(0, 194, 255, 0.12);
            --primary-glow: rgba(0, 194, 255, 0.28);
            --accent: #ff6b35;
            /* construction orange */
            --accent-dim: rgba(255, 107, 53, 0.12);
            --green: #00e5a0;
            /* status green */
            --yellow: #fbbf24;
            --text-hi: #eaf4ff;
            --text-md: #7fa8cc;
            --text-lo: #4a6a88;
            --border: rgba(0, 194, 255, 0.12);
            --border-hi: rgba(0, 194, 255, 0.30);
            --shadow-lg: 0 24px 64px rgba(0, 0, 0, 0.6);
            --shadow-glow: 0 0 48px rgba(0, 194, 255, 0.18);
            --r-xl: 24px;
            --r-lg: 18px;
            --r-md: 12px;
            --r-sm: 8px;
            --cw: 1240px;

            /* Font stack — Arial Unicode MS for full Vietnamese support */
            --f-stack: "Arial Unicode MS", Arial, Tahoma, "Helvetica Neue", Helvetica, sans-serif;
            --f-head: "Arial Unicode MS", Arial, Tahoma, "Helvetica Neue", Helvetica, sans-serif;
            --f-mono: "Arial Unicode MS", Arial, Tahoma, "Helvetica Neue", Helvetica, sans-serif;
            --f-ui: "Arial Unicode MS", Arial, Tahoma, "Helvetica Neue", Helvetica, sans-serif;
            --f-body: "Arial Unicode MS", Arial, Tahoma, "Helvetica Neue", Helvetica, sans-serif;
        }

        /* ── Universal Font Reset – buộc toàn trang dùng Arial Unicode MS ── */
        html,
        body,
        div,
        span,
        p,
        a,
        h1,
        h2,
        h3,
        h4,
        h5,
        h6,
        ul,
        ol,
        li,
        table,
        th,
        td,
        form,
        input,
        button,
        textarea,
        select,
        label,
        section,
        article,
        header,
        footer,
        nav,
        main,
        aside,
        *,
        *::before,
        *::after {
            box-sizing: border-box;
            font-family: "Arial Unicode MS", Arial, Tahoma, "Helvetica Neue", Helvetica, sans-serif !important;
        }

        html,
        body,
        div,
        span {
            margin: 0;
            padding: 0;
        }

        /* Bảo vệ FontAwesome icons — bao gồm cả ::before pseudo-element (FA dùng để render icon) */
        .fa,
        .fas,
        .far,
        .fal,
        .fad,
        .fab,
        .fa-solid,
        .fa-regular,
        .fa-brands,
        [class^="fa-"],
        [class*=" fa-"],
        .fa::before,
        .fas::before,
        .far::before,
        .fal::before,
        .fad::before,
        .fab::before,
        .fa-solid::before,
        .fa-regular::before,
        .fa-brands::before,
        [class^="fa-"]::before,
        [class*=" fa-"]::before {
            font-family: "Font Awesome 6 Free", "Font Awesome 6 Brands", FontAwesome !important;
            font-style: normal !important;
            font-variant: normal !important;
            text-rendering: auto !important;
            -webkit-font-smoothing: antialiased !important;
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
        }

        body {
            font-size: 1rem;
            font-weight: 400;
            background: var(--bg);
            color: var(--text-md);
            overflow-x: hidden;
            letter-spacing: 0.01em;
        }

        a {
            color: inherit;
            text-decoration: none;
        }

        img {
            max-width: 100%;
            display: block;
        }

        strong {
            font-weight: 700;
        }

        .container {
            width: min(var(--cw), calc(100% - 48px));
            margin: 0 auto;
        }

        /* ── GLOBAL BG SYSTEM ── */
        #tech-canvas {
            position: fixed;
            inset: 0;
            z-index: -4;
        }

        .bg-field {
            position: fixed;
            inset: 0;
            z-index: -3;
            background:
                radial-gradient(ellipse 120% 55% at 50% -10%, rgba(0, 194, 255, 0.06) 0%, transparent 65%),
                radial-gradient(ellipse 60% 40% at 100% 30%, rgba(0, 194, 255, 0.04) 0%, transparent 55%),
                radial-gradient(ellipse 50% 35% at 0% 80%, rgba(255, 107, 53, 0.03) 0%, transparent 55%),
                linear-gradient(175deg, #020b14 0%, #030f1c 55%, #02090f 100%);
            pointer-events: none;
        }

        /* Tech grid */
        .grid-field {
            position: fixed;
            inset: 0;
            z-index: -2;
            pointer-events: none;
            background-image:
                linear-gradient(rgba(0, 194, 255, 0.04) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 194, 255, 0.04) 1px, transparent 1px);
            background-size: 52px 52px;
            mask-image: radial-gradient(ellipse 90% 90% at 50% 35%, #000 10%, transparent 75%);
        }

        /* Scanlines */
        .scanlines {
            position: fixed;
            inset: 0;
            z-index: -1;
            pointer-events: none;
            background: repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(0, 0, 0, 0.06) 3px, rgba(0, 0, 0, 0.06) 4px);
            opacity: 0.5;
        }

        /* Ripple canvas for click effect */
        #ripple-canvas {
            position: fixed;
            inset: 0;
            z-index: 3;
            pointer-events: none;
        }

        /* Hex grid overlay canvas */
        #hex-canvas {
            position: fixed;
            inset: 0;
            z-index: -1;
            pointer-events: none;
            opacity: 0.6;
        }

        /* ════ NAV ════ */
        nav {
            position: sticky;
            top: 0;
            z-index: 900;
            background: rgba(2, 11, 20, 0.92);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border-bottom: 1px solid var(--border);
        }

        .nav-inner {
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 70px;
            gap: 24px;
        }

        /* Brand */
        .brand {
            display: inline-flex;
            align-items: center;
            gap: 14px;
        }

        .brand-mark {
            position: relative;
            width: 42px;
            height: 42px;
            border-radius: 10px;
            flex-shrink: 0;
            background: linear-gradient(135deg, rgba(0, 194, 255, 0.15), rgba(0, 194, 255, 0.05));
            border: 1.5px solid rgba(0, 194, 255, 0.35);
            display: grid;
            place-items: center;
            box-shadow: 0 0 20px rgba(0, 194, 255, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.06);
        }

        .brand-mark i {
            color: var(--primary);
            font-size: 18px;
        }

        .brand-mark::before {
            content: '';
            position: absolute;
            inset: -1px;
            border-radius: 11px;
            background: linear-gradient(135deg, rgba(0, 194, 255, 0.4), transparent 60%);
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: exclude;
            padding: 1px;
            pointer-events: none;
        }

        .brand-info {
            display: flex;
            flex-direction: column;
            gap: 1px;
        }

        .brand-name {
            font-family: var(--f-head) !important;
            font-size: 17px;
            font-weight: 800;
            color: var(--text-hi);
            letter-spacing: 0.04em;
            line-height: 1.25 !important;
            text-transform: uppercase;
        }

        .brand-sub {
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 500;
            color: var(--primary);
            letter-spacing: 0.15em;
            text-transform: uppercase;
            opacity: 0.85;
        }

        /* Nav links */
        .nav-links {
            display: flex;
            align-items: center;
            gap: 0;
        }

        .nav-links a {
            font-family: var(--f-ui) !important;
            font-size: 14px;
            font-weight: 600;
            color: var(--text-md);
            padding: 8px 14px;
            border-radius: var(--r-sm);
            letter-spacing: 0.04em;
            text-transform: uppercase;
            font-size: 12.5px;
            transition: color .2s, background .2s;
        }

        .nav-links a:hover,
        .nav-links a.active {
            color: var(--primary);
            background: rgba(0, 194, 255, 0.07);
        }

        .nav-cta {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-left: 8px;
            padding: 9px 20px;
            border-radius: var(--r-sm);
            font-family: var(--f-ui) !important;
            font-size: 12.5px;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            background: var(--primary);
            color: var(--bg) !important;
            box-shadow: 0 4px 20px rgba(0, 194, 255, 0.4);
            transition: transform .2s, box-shadow .2s;
        }

        .nav-cta:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 32px rgba(0, 194, 255, 0.55);
        }

        /* ════ HERO ════ */
        .hero {
            position: relative;
            min-height: calc(100vh - 70px);
            display: flex;
            align-items: center;
            padding: 72px 0 64px;
            overflow: hidden;
        }

        /* Hero background image layer */
        .hero-bg {
            position: absolute;
            inset: 0;
            z-index: 0;
            overflow: hidden;
        }

        .hero-bg img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center 30%;
            opacity: 0.15;
            filter: saturate(1.4) brightness(0.9);
        }

        .hero-bg::before {
            content: '';
            position: absolute;
            inset: 0;
            z-index: 1;
            background:
                linear-gradient(90deg, rgba(2, 11, 20, 0.97) 30%, rgba(2, 11, 20, 0.6) 70%, rgba(2, 11, 20, 0.85) 100%),
                linear-gradient(180deg, rgba(2, 11, 20, 0.3) 0%, transparent 30%, rgba(2, 11, 20, 0.8) 100%);
        }

        /* Decorative corner lines */
        .hero-bg::after {
            content: '';
            position: absolute;
            inset: 0;
            z-index: 2;
            background:
                linear-gradient(90deg, var(--primary) 1px, transparent 1px) 0 0 / 1px 100% no-repeat,
                linear-gradient(0deg, var(--primary) 1px, transparent 1px) 0 100% / 100% 1px no-repeat;
            opacity: 0.08;
            pointer-events: none;
        }

        .hero>.container {
            position: relative;
            z-index: 2;
        }

        .hero-grid {
            display: grid;
            grid-template-columns: 1.1fr 0.9fr;
            gap: 56px;
            align-items: center;
        }

        /* Status line */
        .status-line {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 7px 16px;
            border-radius: 4px;
            background: rgba(0, 229, 160, 0.06);
            border: 1px solid rgba(0, 229, 160, 0.2);
            border-left: 3px solid var(--green);
            font-family: var(--f-mono) !important;
            font-size: 11px;
            font-weight: 500;
            color: var(--green);
            letter-spacing: 0.1em;
            text-transform: uppercase;
            margin-bottom: 28px;
        }

        .status-dot {
            width: 7px;
            height: 7px;
            border-radius: 50%;
            background: var(--green);
            box-shadow: 0 0 8px var(--green);
            animation: blink 2s infinite;
        }

        @keyframes blink {

            0%,
            100% {
                opacity: 1
            }

            50% {
                opacity: .3
            }
        }

        /* Hero headline */
        .hero-eyebrow {
            font-family: var(--f-mono) !important;
            font-size: 11.5px;
            font-weight: 600;
            color: var(--primary);
            letter-spacing: 0.2em;
            text-transform: uppercase;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .hero-eyebrow::before {
            content: '//';
            opacity: 0.5;
        }

        .hero-h1 {
            font-family: var(--f-head) !important;
            font-size: clamp(44px, 6vw, 78px);
            font-weight: 800;
            line-height: 1.15 !important;
            letter-spacing: -0.02em;
            color: var(--text-hi);
            margin-bottom: 16px;
            text-transform: uppercase;
        }

        .hero-h1 .line-accent {
            display: block;
            background: linear-gradient(100deg, #00c2ff 0%, #00e5ff 40%, #7dd3fc 80%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 0 24px rgba(0, 194, 255, 0.35));
        }

        .hero-h1 .line-dim {
            font-size: 0.52em;
            letter-spacing: 0.06em;
            color: var(--text-md);
            -webkit-text-fill-color: var(--text-md);
            display: block;
            margin-top: 12px;
            line-height: 1.3 !important;
        }

        .hero-role {
            font-family: var(--f-mono) !important;
            font-size: 13px;
            font-weight: 500;
            color: var(--accent);
            letter-spacing: 0.08em;
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 24px;
        }

        .hero-role::before {
            content: '>_';
            font-size: 16px;
            opacity: 0.7;
        }

        .hero-desc {
            font-family: var(--f-body) !important;
            font-size: 17px;
            font-weight: 400;
            line-height: 1.8;
            color: var(--text-md);
            max-width: 560px;
            margin-bottom: 32px;
        }

        .hero-desc b {
            color: var(--text-hi);
            font-weight: 600;
        }

        .hero-desc span.tag {
            color: var(--primary);
            font-weight: 600;
        }

        /* Tech chips */
        .chip-row {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-bottom: 36px;
        }

        .chip {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 13px;
            border-radius: 4px;
            background: rgba(0, 194, 255, 0.06);
            border: 1px solid rgba(0, 194, 255, 0.16);
            font-family: var(--f-mono) !important;
            font-size: 11.5px;
            font-weight: 500;
            color: var(--text-md);
            letter-spacing: 0.06em;
            text-transform: uppercase;
            transition: border-color .2s, color .2s, background .2s;
        }

        .chip i {
            color: var(--primary);
            font-size: 10px;
        }

        .chip:hover {
            border-color: rgba(0, 194, 255, 0.38);
            color: var(--primary);
            background: rgba(0, 194, 255, 0.1);
        }

        /* Buttons */
        .btn-row {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-bottom: 44px;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 14px 28px;
            border-radius: var(--r-sm);
            font-family: var(--f-ui) !important;
            font-size: 13px;
            font-weight: 700;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            transition: transform .25s, box-shadow .25s;
        }

        .btn:hover {
            transform: translateY(-3px);
        }

        .btn-fill {
            background: var(--primary);
            color: var(--bg);
            box-shadow: 0 6px 24px rgba(0, 194, 255, 0.45);
        }

        .btn-fill:hover {
            box-shadow: 0 12px 36px rgba(0, 194, 255, 0.6);
        }

        .btn-ghost {
            color: var(--text-hi);
            border: 1.5px solid var(--border-hi);
            background: rgba(0, 194, 255, 0.04);
        }

        .btn-ghost:hover {
            border-color: var(--primary);
            color: var(--primary);
            background: rgba(0, 194, 255, 0.08);
        }

        /* Hero stats */
        .hero-kpi {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
        }

        .kpi-box {
            padding: 16px 18px;
            border-radius: var(--r-md);
            background: rgba(4, 15, 30, 0.8);
            border: 1px solid var(--border);
            border-top: 2px solid rgba(0, 194, 255, 0.3);
            transition: border-color .3s, transform .3s;
        }

        .kpi-box:hover {
            border-color: var(--border-hi);
            transform: translateY(-4px);
        }

        .kpi-val {
            font-family: var(--f-head) !important;
            font-size: 20px;
            font-weight: 800;
            color: var(--primary);
            letter-spacing: 0.04em;
            line-height: 1;
            margin-bottom: 6px;
            text-shadow: 0 0 16px rgba(0, 194, 255, 0.4);
        }

        .kpi-lbl {
            font-family: var(--f-mono) !important;
            font-size: 10.5px;
            color: var(--text-lo);
            letter-spacing: 0.08em;
            text-transform: uppercase;
            line-height: 1.5;
        }

        /* ── PROFILE CARD ── */
        .profile-card {
            position: relative;
            border-radius: var(--r-xl);
            background: var(--bg-card);
            border: 1px solid var(--border-hi);
            box-shadow: var(--shadow-lg), var(--shadow-glow);
            overflow: hidden;
            transform-style: preserve-3d;
        }

        /* Corner accent lines */
        .profile-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 60px;
            height: 60px;
            z-index: 2;
            pointer-events: none;
            background:
                linear-gradient(90deg, var(--primary) 2px, transparent 2px) top left / 100% 100%,
                linear-gradient(180deg, var(--primary) 2px, transparent 2px) top left / 100% 100%;
            opacity: 0.6;
            border-radius: var(--r-xl) 0 0 0;
        }

        .profile-card::after {
            content: '';
            position: absolute;
            bottom: 0;
            right: 0;
            width: 60px;
            height: 60px;
            z-index: 2;
            pointer-events: none;
            background:
                linear-gradient(270deg, var(--primary) 2px, transparent 2px) bottom right / 100% 100%,
                linear-gradient(0deg, var(--primary) 2px, transparent 2px) bottom right / 100% 100%;
            opacity: 0.6;
            border-radius: 0 0 var(--r-xl) 0;
        }

        .profile-hdr {
            padding: 12px 16px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid var(--border);
            background: rgba(0, 194, 255, 0.03);
        }

        .profile-badge {
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 600;
            color: var(--accent);
            letter-spacing: 0.12em;
            text-transform: uppercase;
            display: flex;
            align-items: center;
            gap: 7px;
        }

        .profile-badge i {
            color: var(--accent);
            font-size: 11px;
        }

        .live-badge {
            display: flex;
            align-items: center;
            gap: 6px;
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 500;
            color: var(--green);
            letter-spacing: 0.1em;
        }

        .profile-img-wrap {
            position: relative;
            height: 380px;
            overflow: hidden;
        }

        .profile-img-wrap img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center top;
            transition: transform .7s ease;
            filter: saturate(0.9);
        }

        .profile-card:hover .profile-img-wrap img {
            transform: scale(1.04);
        }

        .profile-img-grad {
            position: absolute;
            inset: 0;
            background: linear-gradient(0deg, rgba(2, 11, 20, 0.92) 0%, rgba(2, 11, 20, 0.05) 50%, transparent 100%);
        }

        .profile-tag {
            position: absolute;
            bottom: 16px;
            left: 16px;
            right: 16px;
        }

        .profile-tag h3 {
            font-family: var(--f-head) !important;
            font-size: 21px;
            font-weight: 800;
            color: var(--text-hi);
            letter-spacing: 0.04em;
            text-transform: uppercase;
            margin-bottom: 4px;
            line-height: 1.25 !important;
        }

        .profile-tag p {
            font-family: var(--f-mono) !important;
            font-size: 11px;
            color: var(--primary);
            letter-spacing: 0.1em;
        }

        .profile-data {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1px;
            background: var(--border);
        }

        .pd-cell {
            padding: 12px 15px;
            background: var(--bg-card);
        }

        .pd-lbl {
            font-family: var(--f-mono) !important;
            font-size: 9.5px;
            font-weight: 600;
            letter-spacing: 0.14em;
            text-transform: uppercase;
            color: var(--text-lo);
            margin-bottom: 4px;
        }

        .pd-val {
            font-family: var(--f-ui) !important;
            font-size: 13px;
            font-weight: 600;
            color: var(--text-hi);
            line-height: 1.4;
        }

        /* ════ TICKER STRIP ════ */
        .ticker-strip {
            overflow: hidden;
            padding: 16px 0;
            border-top: 1px solid var(--border);
            border-bottom: 1px solid var(--border);
            background: rgba(0, 11, 20, 0.7);
        }

        .ticker-track {
            display: flex;
            gap: 52px;
            animation: ticker 30s linear infinite;
            width: max-content;
        }

        .ticker-item {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            font-family: var(--f-mono) !important;
            font-size: 11px;
            font-weight: 500;
            color: var(--text-lo);
            letter-spacing: 0.1em;
            text-transform: uppercase;
            white-space: nowrap;
        }

        .ticker-item i {
            color: var(--primary);
        }

        .ticker-sep {
            color: rgba(0, 194, 255, 0.2);
            font-size: 8px;
        }

        @keyframes ticker {
            from {
                transform: translateX(0)
            }

            to {
                transform: translateX(-50%)
            }
        }

        /* ════ SECTION BASE ════ */
        section {
            padding: 96px 0;
            position: relative;
        }

        .s-kicker {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 10px;
            font-family: var(--f-mono) !important;
            font-size: 11px;
            font-weight: 600;
            color: var(--primary);
            letter-spacing: 0.2em;
            text-transform: uppercase;
        }

        .s-kicker::before {
            content: '//';
            opacity: 0.45;
        }

        .s-title {
            font-family: var(--f-head) !important;
            font-size: clamp(30px, 4vw, 46px);
            font-weight: 800;
            color: var(--text-hi);
            letter-spacing: -0.02em;
            line-height: 1.25 !important;
            text-transform: uppercase;
            margin-bottom: 14px;
        }

        .s-desc {
            font-family: var(--f-body) !important;
            font-size: 16.5px;
            color: var(--text-md);
            line-height: 1.8;
            max-width: 580px;
        }

        .s-hdr {
            margin-bottom: 52px;
        }

        /* Section divider line */
        .s-divider {
            width: 48px;
            height: 3px;
            border-radius: 2px;
            background: linear-gradient(90deg, var(--primary), transparent);
            margin-bottom: 18px;
        }

        /* ════ CARD ════ */
        .card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: var(--r-lg);
            position: relative;
            overflow: hidden;
            transition: transform .32s ease, border-color .32s ease, box-shadow .32s ease;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 55%;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(0, 194, 255, 0.75), transparent);
            transition: left .55s ease;
        }

        .card:hover {
            transform: translateY(-6px);
            border-color: var(--border-hi);
            box-shadow: var(--shadow-lg), var(--shadow-glow);
        }

        .card:hover::before {
            left: 145%;
        }

        .card-p {
            padding: 26px;
        }

        /* ════ ABOUT ════ */
        #about {
            background: linear-gradient(180deg, transparent, rgba(0, 194, 255, 0.02) 50%, transparent);
        }

        .about-grid {
            display: grid;
            grid-template-columns: 1.3fr 0.7fr;
            gap: 24px;
            align-items: start;
        }

        .about-banner {
            position: relative;
            border-radius: var(--r-lg);
            overflow: hidden;
            height: 200px;
            margin-bottom: 22px;
            border: 1px solid var(--border-hi);
        }

        .about-banner img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: saturate(1.2) brightness(0.8);
        }

        .about-banner::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(90deg, rgba(2, 11, 20, 0.7) 0%, transparent 60%, rgba(2, 11, 20, 0.5) 100%);
        }

        .about-banner-label {
            position: absolute;
            bottom: 14px;
            left: 16px;
            z-index: 2;
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 600;
            color: var(--primary);
            letter-spacing: 0.14em;
            text-transform: uppercase;
        }

        .about-body p {
            font-family: var(--f-body) !important;
            font-size: 16px;
            line-height: 1.9;
            color: #9ab5cc;
            margin-bottom: 16px;
        }

        .about-body p:last-child {
            margin-bottom: 0;
        }

        .hl {
            color: var(--primary);
            font-weight: 600;
        }

        .hl-w {
            color: var(--text-hi);
            font-weight: 600;
        }

        .hl-o {
            color: var(--accent);
            font-weight: 600;
        }

        /* About sidebar specs */
        .about-specs {
            display: grid;
            gap: 12px;
        }

        .spec-card {
            padding: 16px 18px;
            border-radius: var(--r-md);
            background: rgba(0, 194, 255, 0.04);
            border: 1px solid var(--border);
            border-left: 3px solid var(--primary);
            transition: border-color .25s, background .25s;
        }

        .spec-card:hover {
            border-color: var(--border-hi);
            background: rgba(0, 194, 255, 0.07);
        }

        .spec-icon {
            font-size: 14px;
            color: var(--primary);
            margin-bottom: 8px;
        }

        .spec-title {
            font-family: var(--f-ui) !important;
            font-size: 14px;
            font-weight: 700;
            color: var(--text-hi);
            margin-bottom: 5px;
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }

        .spec-desc {
            font-family: var(--f-body) !important;
            font-size: 13.5px;
            color: var(--text-md);
            line-height: 1.6;
        }

        /* ════ FIELD IMAGE GALLERY ════ */
        .gallery {
            padding: 0 0 88px;
        }

        .gallery-grid {
            display: grid;
            grid-template-columns: 1.1fr 0.9fr;
            gap: 20px;
        }

        .gal-card {
            position: relative;
            border-radius: var(--r-lg);
            overflow: hidden;
            border: 1px solid var(--border-hi);
            box-shadow: var(--shadow-lg);
            cursor: pointer;
        }

        .gal-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform .65s ease, filter .65s ease;
        }

        .gal-card:hover img {
            transform: scale(1.06);
            filter: brightness(1.05) saturate(1.15);
        }

        .gal-card.tall {
            height: 440px;
        }

        .gal-right {
            display: grid;
            grid-template-rows: 1fr 1fr;
            gap: 20px;
        }

        .gal-card.short {
            height: auto;
        }

        .gal-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(0deg, rgba(2, 11, 20, 0.92) 0%, rgba(2, 11, 20, 0.1) 55%, transparent 100%);
            transition: background .4s;
        }

        .gal-card:hover .gal-overlay {
            background: linear-gradient(0deg, rgba(2, 11, 20, 0.85) 0%, rgba(2, 11, 20, 0.05) 60%, transparent 100%);
        }

        .gal-info {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 20px 22px;
        }

        .gal-tag {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            margin-bottom: 8px;
            padding: 4px 11px;
            border-radius: 3px;
            background: rgba(0, 194, 255, 0.15);
            border: 1px solid rgba(0, 194, 255, 0.25);
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 600;
            color: var(--primary);
            letter-spacing: 0.12em;
            text-transform: uppercase;
        }

        .gal-title {
            font-family: var(--f-head) !important;
            font-size: 18px;
            font-weight: 700;
            color: var(--text-hi);
            text-transform: uppercase;
            letter-spacing: 0.04em;
            margin-bottom: 4px;
        }

        .gal-sub {
            font-family: var(--f-mono) !important;
            font-size: 11px;
            color: var(--text-md);
            letter-spacing: 0.06em;
        }

        /* ════ TOOLS ════ */
        #tools {
            background: linear-gradient(180deg, transparent, rgba(0, 194, 255, 0.015) 50%, transparent);
        }

        .tools-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 18px;
        }

        .tool-card {
            padding: 24px;
            border-radius: var(--r-lg);
            background: rgba(4, 15, 30, 0.8);
            border: 1px solid var(--border);
            position: relative;
            overflow: hidden;
            transition: transform .3s, border-color .3s, box-shadow .3s;
        }

        .tool-card::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--primary), var(--primary-2, #06b6d4));
            transform: scaleX(0);
            transition: transform .35s;
        }

        .tool-card:hover {
            transform: translateY(-6px);
            border-color: var(--border-hi);
            box-shadow: var(--shadow-lg), var(--shadow-glow);
        }

        .tool-card:hover::after {
            transform: scaleX(1);
        }

        .tool-num {
            font-family: var(--f-mono) !important;
            font-size: 11px;
            font-weight: 600;
            color: var(--text-lo);
            letter-spacing: 0.12em;
            margin-bottom: 14px;
        }

        .tool-icon-wrap {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: grid;
            place-items: center;
            font-size: 22px;
            margin-bottom: 16px;
        }

        .ti-blue {
            background: rgba(0, 194, 255, 0.08);
            border: 1px solid rgba(0, 194, 255, 0.18);
            color: var(--primary);
        }

        .ti-orange {
            background: rgba(255, 107, 53, 0.08);
            border: 1px solid rgba(255, 107, 53, 0.18);
            color: var(--accent);
        }

        .ti-green {
            background: rgba(0, 229, 160, 0.08);
            border: 1px solid rgba(0, 229, 160, 0.18);
            color: var(--green);
        }

        .ti-yellow {
            background: rgba(251, 191, 36, 0.08);
            border: 1px solid rgba(251, 191, 36, 0.18);
            color: var(--yellow);
        }

        .tool-card h3 {
            font-family: var(--f-head) !important;
            font-size: 16px;
            font-weight: 700;
            color: var(--text-hi);
            letter-spacing: 0.04em;
            text-transform: uppercase;
            margin-bottom: 8px;
        }

        .tool-card p {
            font-family: var(--f-body) !important;
            font-size: 14px;
            line-height: 1.7;
            color: var(--text-md);
            margin-bottom: 16px;
        }

        .tool-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
        }

        .t-tag {
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 500;
            padding: 4px 9px;
            border-radius: 4px;
            background: rgba(0, 194, 255, 0.06);
            border: 1px solid rgba(0, 194, 255, 0.12);
            color: var(--text-md);
            letter-spacing: 0.06em;
        }

        /* ════ SKILLS ════ */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 18px;
        }

        .skill-card {
            padding: 22px;
        }

        .skill-top {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 14px;
        }

        .skill-ico {
            width: 36px;
            height: 36px;
            border-radius: 9px;
            display: grid;
            place-items: center;
            background: rgba(0, 194, 255, 0.07);
            border: 1px solid rgba(0, 194, 255, 0.14);
            color: var(--primary);
            font-size: 14px;
            flex-shrink: 0;
        }

        .skill-meta {
            flex: 1;
            min-width: 0;
        }

        .skill-name {
            font-family: var(--f-ui) !important;
            font-size: 14px;
            font-weight: 700;
            color: var(--text-hi);
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }

        .skill-pct {
            font-family: var(--f-mono) !important;
            font-size: 14px;
            font-weight: 600;
            color: var(--primary);
        }

        .pbar {
            width: 100%;
            height: 6px;
            border-radius: 99px;
            overflow: hidden;
            background: rgba(0, 194, 255, 0.07);
            margin-bottom: 10px;
        }

        .pfill {
            height: 100%;
            width: 0;
            border-radius: inherit;
            background: linear-gradient(90deg, #0056a3, #00c2ff, #80e8ff);
            box-shadow: 0 0 12px rgba(0, 194, 255, 0.5);
            transition: width 1.4s cubic-bezier(.2, .8, .2, 1);
        }

        .skill-note {
            font-family: var(--f-body) !important;
            font-size: 13.5px;
            line-height: 1.65;
            color: var(--text-md);
        }

        /* ════ EXPERIENCE ════ */
        #experience {
            background: linear-gradient(180deg, transparent, rgba(255, 107, 53, 0.015) 50%, transparent);
        }

        .exp-wrap {
            display: grid;
            gap: 20px;
        }

        .exp-card {
            padding: 28px 28px 28px 32px;
        }

        .exp-card::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 3px;
            background: linear-gradient(180deg, var(--primary), rgba(0, 194, 255, 0.15));
            border-radius: 0 2px 2px 0;
        }

        .exp-meta {
            display: flex;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 14px;
        }

        .exp-period {
            display: inline-flex;
            align-items: center;
            gap: 7px;
            padding: 4px 12px;
            border-radius: 4px;
            background: rgba(255, 107, 53, 0.07);
            border: 1px solid rgba(255, 107, 53, 0.18);
            font-family: var(--f-mono) !important;
            font-size: 11px;
            font-weight: 600;
            color: var(--accent);
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }

        .exp-current {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 600;
            color: var(--green);
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }

        .exp-title {
            font-family: var(--f-head) !important;
            font-size: 22px;
            font-weight: 800;
            color: var(--text-hi);
            text-transform: uppercase;
            letter-spacing: 0.02em;
            margin-bottom: 6px;
            line-height: 1.2;
        }

        .exp-company {
            font-family: var(--f-mono) !important;
            font-size: 12px;
            font-weight: 600;
            color: var(--primary);
            letter-spacing: 0.08em;
            margin-bottom: 16px;
        }

        .exp-desc {
            font-family: var(--f-body) !important;
            font-size: 15.5px;
            line-height: 1.9;
            color: #9ab5cc;
        }

        .exp-chips {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 18px;
        }

        .echip {
            font-family: var(--f-mono) !important;
            font-size: 10.5px;
            font-weight: 500;
            padding: 5px 11px;
            border-radius: 4px;
            background: rgba(0, 194, 255, 0.06);
            border: 1px solid rgba(0, 194, 255, 0.12);
            color: var(--text-md);
            letter-spacing: 0.06em;
            text-transform: uppercase;
        }

        /* ════ PROJECTS ════ */
        .proj-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
        }

        .proj-card {
            padding: 0;
            overflow: hidden;
        }

        .proj-media {
            position: relative;
            height: 215px;
            overflow: hidden;
        }

        .proj-media img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform .6s, filter .6s;
        }

        .proj-card:hover .proj-media img {
            transform: scale(1.08);
            filter: saturate(1.2) brightness(1.05);
        }

        .proj-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(180deg, transparent 25%, rgba(2, 11, 20, 0.82) 100%);
        }

        .proj-badge {
            position: absolute;
            top: 12px;
            left: 12px;
            padding: 5px 10px;
            border-radius: 4px;
            background: rgba(2, 11, 20, 0.88);
            border: 1px solid var(--border-hi);
            font-family: var(--f-mono) !important;
            font-size: 9.5px;
            font-weight: 600;
            color: var(--primary);
            letter-spacing: 0.12em;
            text-transform: uppercase;
            backdrop-filter: blur(8px);
        }

        .proj-body {
            padding: 20px;
        }

        .proj-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            margin-bottom: 11px;
        }

        .ptag {
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 600;
            color: var(--primary);
            padding: 4px 9px;
            border-radius: 4px;
            background: rgba(0, 194, 255, 0.07);
            border: 1px solid rgba(0, 194, 255, 0.14);
            letter-spacing: 0.06em;
            text-transform: uppercase;
        }

        .proj-title {
            font-family: var(--f-head) !important;
            font-size: 18px;
            font-weight: 700;
            color: var(--text-hi);
            text-transform: uppercase;
            letter-spacing: 0.03em;
            margin-bottom: 9px;
        }

        .proj-desc {
            font-family: var(--f-body) !important;
            font-size: 14px;
            line-height: 1.75;
            color: #7fa8cc;
            margin-bottom: 16px;
        }

        .proj-link {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            font-family: var(--f-mono) !important;
            font-size: 11px;
            font-weight: 600;
            color: var(--primary);
            letter-spacing: 0.1em;
            text-transform: uppercase;
            transition: gap .2s;
        }

        .proj-link:hover {
            gap: 12px;
        }

        /* ════ CERTS ════ */
        .cert-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
        }

        .cert-card {
            padding: 24px;
            display: grid;
            gap: 18px;
        }

        .cert-head {
            display: flex;
            gap: 16px;
            align-items: flex-start;
        }

        .cert-ico {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: grid;
            place-items: center;
            font-size: 20px;
            flex-shrink: 0;
            background: rgba(255, 107, 53, 0.07);
            border: 1px solid rgba(255, 107, 53, 0.18);
            color: var(--accent);
        }

        .cert-title {
            font-family: var(--f-head) !important;
            font-size: 17px;
            font-weight: 700;
            color: var(--text-hi);
            text-transform: uppercase;
            letter-spacing: 0.03em;
            margin-bottom: 4px;
        }

        .cert-issuer {
            font-family: var(--f-mono) !important;
            font-size: 11px;
            font-weight: 600;
            color: var(--primary);
            letter-spacing: 0.1em;
            text-transform: uppercase;
            margin-bottom: 8px;
        }

        .cert-desc {
            font-family: var(--f-body) !important;
            font-size: 14px;
            line-height: 1.75;
            color: var(--text-md);
        }

        .cert-img-wrap {
            overflow: hidden;
            border-radius: var(--r-md);
            border: 1px solid var(--border);
        }

        .cert-img {
            width: 100%;
            height: 195px;
            object-fit: contain;
            background: rgba(2, 8, 20, 0.6);
            transition: transform .45s;
        }

        .cert-card:hover .cert-img {
            transform: scale(1.03);
        }

        /* ════ CONTACT ════ */
        #contact {
            background: linear-gradient(180deg, transparent, rgba(0, 194, 255, 0.02) 50%, transparent);
        }

        .contact-grid {
            display: grid;
            grid-template-columns: 1.2fr 0.8fr;
            gap: 22px;
            align-items: start;
        }

        .contact-lead p {
            font-family: var(--f-body) !important;
            font-size: 16px;
            line-height: 1.85;
            color: #9ab5cc;
            margin-bottom: 28px;
        }

        .contact-list {
            display: grid;
            gap: 11px;
        }

        .contact-row {
            display: flex;
            align-items: center;
            gap: 14px;
            padding: 15px 18px;
            border-radius: var(--r-md);
            background: rgba(4, 15, 30, 0.85);
            border: 1px solid var(--border);
            transition: border-color .25s;
        }

        .contact-row:hover {
            border-color: var(--border-hi);
        }

        .contact-ico {
            width: 42px;
            height: 42px;
            border-radius: 10px;
            display: grid;
            place-items: center;
            background: rgba(0, 194, 255, 0.08);
            border: 1px solid rgba(0, 194, 255, 0.15);
            color: var(--primary);
            font-size: 15px;
            flex-shrink: 0;
        }

        .c-lbl {
            font-family: var(--f-mono) !important;
            font-size: 10px;
            font-weight: 600;
            letter-spacing: 0.12em;
            text-transform: uppercase;
            color: var(--text-lo);
            margin-bottom: 3px;
        }

        .c-val {
            font-family: var(--f-ui) !important;
            font-size: 14px;
            font-weight: 600;
            color: var(--text-hi);
        }

        .social-row {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 22px;
        }

        .soc-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 10px 18px;
            border-radius: var(--r-sm);
            background: rgba(4, 15, 30, 0.85);
            border: 1px solid var(--border);
            font-family: var(--f-ui) !important;
            font-size: 13px;
            font-weight: 700;
            color: var(--text-md);
            letter-spacing: 0.06em;
            text-transform: uppercase;
            transition: transform .2s, border-color .2s, color .2s, box-shadow .2s;
        }

        .soc-btn:hover {
            transform: translateY(-2px);
            border-color: var(--border-hi);
            color: var(--primary);
            box-shadow: var(--shadow-glow);
        }

        .contact-side {
            display: grid;
            gap: 14px;
        }

        .mini-panel {
            padding: 20px;
            border-radius: var(--r-lg);
            background: rgba(4, 15, 30, 0.85);
            border: 1px solid var(--border);
            transition: border-color .25s;
        }

        .mini-panel:hover {
            border-color: var(--border-hi);
        }

        .mp-ico {
            width: 36px;
            height: 36px;
            border-radius: 9px;
            display: grid;
            place-items: center;
            background: rgba(0, 194, 255, 0.08);
            border: 1px solid rgba(0, 194, 255, 0.14);
            color: var(--primary);
            margin-bottom: 12px;
            font-size: 14px;
        }

        .mini-panel h3 {
            font-family: var(--f-head) !important;
            font-size: 15px;
            font-weight: 700;
            color: var(--text-hi);
            text-transform: uppercase;
            letter-spacing: 0.04em;
            margin-bottom: 7px;
        }

        .mini-panel p {
            font-family: var(--f-body) !important;
            font-size: 13.5px;
            line-height: 1.75;
            color: var(--text-md);
        }

        /* ════ FOOTER ════ */
        footer {
            border-top: 1px solid var(--border);
            background: rgba(2, 11, 20, 0.96);
            padding: 28px 0;
        }

        .foot-inner {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 16px;
            flex-wrap: wrap;
        }

        .foot-brand {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .foot-mark {
            width: 30px;
            height: 30px;
            border-radius: 7px;
            background: linear-gradient(135deg, rgba(0, 194, 255, 0.2), rgba(0, 194, 255, 0.06));
            border: 1.5px solid rgba(0, 194, 255, 0.3);
            display: grid;
            place-items: center;
            color: var(--primary);
            font-size: 13px;
        }

        .foot-name {
            font-family: var(--f-head) !important;
            font-size: 14px;
            font-weight: 700;
            color: var(--text-hi);
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }

        .foot-note {
            font-family: var(--f-mono) !important;
            font-size: 11px;
            color: var(--text-lo);
            letter-spacing: 0.08em;
        }

        /* ════ ANIMATIONS ════ */
        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity .7s ease, transform .7s ease;
        }

        .reveal.in {
            opacity: 1;
            transform: translateY(0);
        }

        /* ════ RESPONSIVE ════ */
        @media (max-width: 1100px) {

            .hero-grid,
            .about-grid,
            .contact-grid,
            .gallery-grid {
                grid-template-columns: 1fr;
            }

            .proj-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .cert-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .tools-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .gal-right {
                grid-template-rows: unset;
                grid-template-columns: 1fr 1fr;
            }

            .gal-card.tall {
                height: 380px;
            }
        }

        @media (max-width: 820px) {
            section {
                padding: 64px 0;
            }

            .skills-grid,
            .cert-grid {
                grid-template-columns: 1fr;
            }

            .proj-grid {
                grid-template-columns: 1fr;
            }

            .tools-grid {
                grid-template-columns: 1fr;
            }

            .nav-inner {
                flex-direction: column;
                height: auto;
                padding: 12px 0;
                gap: 10px;
            }

            .nav-links {
                flex-wrap: wrap;
                justify-content: center;
            }

            .nav-cta {
                display: none;
            }

            .hero-kpi {
                grid-template-columns: 1fr;
            }

            .profile-img-wrap {
                height: 300px;
            }
        }

        @media (max-width: 560px) {
            .hero-h1 {
                font-size: 36px;
            }

            .btn-row {
                flex-direction: column;
            }

            .btn {
                justify-content: center;
            }

            .gal-right {
                grid-template-columns: 1fr;
            }
        }

        /* ════ CATEGORIZED PROJECTS TABS ════ */
        .tab-container {
            margin-top: 24px;
        }

        .tab-nav {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 12px;
            margin-bottom: 40px;
            border-bottom: 1px solid var(--border);
            padding-bottom: 20px;
        }

        .tab-btn {
            background: transparent;
            border: 1px solid var(--border);
            color: var(--text-md);
            padding: 10px 24px;
            border-radius: var(--r-sm);
            font-family: var(--f-ui) !important;
            font-size: 13px;
            font-weight: 700;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.25s ease;
        }

        .tab-btn:hover {
            border-color: var(--border-hi);
            color: var(--primary);
            background: rgba(0, 194, 255, 0.05);
        }

        .tab-btn.active {
            background: var(--primary);
            color: var(--bg);
            border-color: var(--primary);
            box-shadow: 0 0 20px rgba(0, 194, 255, 0.35);
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: grid;
            animation: tabFadeIn 0.45s ease;
        }

        @keyframes tabFadeIn {
            from {
                opacity: 0;
                transform: translateY(15px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ════ LIGHTBOX / ZOOM MODAL ════ */
        .lightbox {
            position: fixed;
            inset: 0;
            background: rgba(2, 11, 20, 0.96);
            backdrop-filter: blur(12px);
            z-index: 2000;
            display: none;
            align-items: center;
            justify-content: center;
            padding: 24px;
        }

        .lightbox.active {
            display: flex;
        }

        .lightbox-content {
            position: relative;
            max-width: 90%;
            max-height: 85vh;
            box-shadow: var(--shadow-lg);
            border-radius: var(--r-md);
            border: 1px solid var(--border-hi);
            overflow: hidden;
            background: var(--bg-card);
            animation: lightboxScale 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        @keyframes lightboxScale {
            from {
                transform: scale(0.9);
                opacity: 0;
            }

            to {
                transform: scale(1);
                opacity: 1;
            }
        }

        .lightbox-content img {
            max-width: 100%;
            max-height: 70vh;
            object-fit: contain;
            display: block;
        }

        .lightbox-caption {
            padding: 16px 20px;
            background: var(--bg-2);
            color: var(--text-hi);
            font-family: var(--f-body) !important;
            font-size: 14.5px;
            border-top: 1px solid var(--border);
            text-align: center;
        }

        .lightbox-close {
            position: absolute;
            top: 24px;
            right: 24px;
            background: rgba(4, 15, 30, 0.85);
            border: 1px solid var(--border-hi);
            color: var(--text-hi);
            width: 44px;
            height: 44px;
            border-radius: 50%;
            font-size: 20px;
            display: grid;
            place-items: center;
            cursor: pointer;
            transition: all 0.25s ease;
            z-index: 2001;
        }

        .lightbox-close:hover {
            background: var(--accent);
            border-color: var(--accent);
            color: var(--text-hi);
            transform: rotate(90deg);
        }

        /* Lightbox zoomable trigger style */
        .zoomable {
            cursor: zoom-in;
        }
    </style>
</head>

<body>

    <canvas id="tech-canvas"></canvas>
    <canvas id="hex-canvas"></canvas>
    <canvas id="ripple-canvas"></canvas>
    <div class="bg-field"></div>
    <div class="grid-field"></div>
    <div class="scanlines"></div>


    <!-- ══════════════ NAV ══════════════ -->
    <nav>
        <div class="container nav-inner">
            <a href="#home" class="brand">
                <div class="brand-mark"><i class="fa-solid fa-building-columns"></i></div>
                <div class="brand-info">
                    <span class="brand-name">Vũ Ngọc Phát</span>
                    <span class="brand-sub">BIM.ENG // CONST.MGR</span>
                </div>
            </a>
            <div class="nav-links">
                <a href="#about">About</a>
                <a href="#tools">Tools</a>
                <a href="#skills">Skills</a>
                <a href="#experience">Experience</a>
                <a href="#projects">Projects</a>
                <a href="#certificates">Certs</a>
                <a href="#contact" class="nav-cta"><i class="fa-solid fa-paper-plane"></i> Contact</a>
            </div>
        </div>
    </nav>

    <!-- ══════════════ HERO ══════════════ -->
    <header class="hero" id="home">
        <div class="hero-bg">
            <img src="images/model-revit-van-phong.jpg" alt="BIM Engineering background" />
        </div>

        <div class="container hero-grid">
            <!-- Copy -->
            <div class="reveal">
                <div class="status-line">
                    <span class="status-dot"></span>
                    Available for BIM Projects &amp; Collaboration
                </div>

                <div class="hero-eyebrow">BIM Engineer · Construction Manager</div>

                <h1 class="hero-h1">
                    <span class="line-accent">Vũ Ngọc Phát</span>
                    <span class="line-dim">Infrastructure · Digital · Field</span>
                </h1>

                <div class="hero-role">
                    Kỹ sư R&amp;D Tekla // Kỹ sư R&amp;D Revit // Kỹ thuật hiện trường
                </div>

                <p class="hero-desc">
                    Kỹ sư <b>BIM hạ tầng giao thông</b> với 3 năm kinh nghiệm thực tế tại các dự án trọng điểm quốc gia
                    — triển khai mô hình
                    <span class="tag">LOD 300–400</span>, kiểm soát tiến độ
                    <span class="tag">4D/5D</span> và tự động hóa quy trình với
                    <span class="tag">Python, Dynamo &amp; Grasshopper</span>.
                </p>

                <div class="chip-row">
                    <span class="chip"><i class="fa-solid fa-cube"></i>Tekla Structures</span>
                    <span class="chip"><i class="fa-solid fa-layer-group"></i>Revit</span>
                    <span class="chip"><i class="fa-solid fa-road"></i>Civil 3D</span>
                    <span class="chip"><i class="fa-solid fa-magnifying-glass-chart"></i>Navisworks</span>
                    <span class="chip"><i class="fa-solid fa-network-wired"></i>Dynamo</span>
                    <span class="chip"><i class="fa-brands fa-python"></i>Python</span>
                </div>

                <div class="btn-row">
                    <a href="#projects" class="btn btn-fill"><i class="fa-solid fa-diagram-project"></i>Xem mô hình
                        3D</a>
                    <a href="#contact" class="btn btn-ghost"><i class="fa-solid fa-paper-plane"></i>Kết nối ngay</a>
                </div>

                <div class="hero-kpi">
                    <div class="kpi-box">
                        <div class="kpi-val">LOD 400</div>
                        <div class="kpi-lbl">Kết cấu &amp; biện pháp thi công cầu</div>
                    </div>
                    <div class="kpi-box">
                        <div class="kpi-val">4D / 5D</div>
                        <div class="kpi-lbl">Primavera P6 &amp; Fuzor integration</div>
                    </div>
                    <div class="kpi-box">
                        <div class="kpi-val">AUTO</div>
                        <div class="kpi-lbl">Python &amp; Grasshopper Tekla API</div>
                    </div>
                </div>
            </div>

            <!-- Profile -->
            <div class="profile-card reveal" id="profilePanel">
                <div class="profile-hdr">
                    <div class="profile-badge">
                        <i class="fa-solid fa-hard-hat"></i>
                        BIM + Field Engineering
                    </div>
                    <div class="live-badge">
                        <span class="status-dot"></span>
                        Sẵn sàng
                    </div>
                </div>

                <div class="profile-img-wrap">
                    <img src="images/chandung.jpg" alt="Vũ Ngọc Phát" />
                    <div class="profile-img-grad"></div>
                    <div class="profile-tag">
                        <h3>Vũ Ngọc Phát</h3>
                        <p><i class="fa-solid fa-location-dot"></i>&nbsp; Hải Hậu, Ninh Bình, Việt Nam</p>
                    </div>
                </div>

                <div class="profile-data">
                    <div class="pd-cell">
                        <div class="pd-lbl">Ngày sinh</div>
                        <div class="pd-val">27/10/2000</div>
                    </div>
                    <div class="pd-cell">
                        <div class="pd-lbl">Liên hệ</div>
                        <div class="pd-val">0825.632.226</div>
                    </div>
                    <div class="pd-cell">
                        <div class="pd-lbl">Chuyên môn</div>
                        <div class="pd-val">Kỹ sư R&amp;D Tekla / Revit</div>
                    </div>
                    <div class="pd-cell">
                        <div class="pd-lbl">Định hướng</div>
                        <div class="pd-val">BIM Automation &amp; Field Control</div>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- ══════════════ TICKER ══════════════ -->
    <div class="ticker-strip">
        <div class="ticker-track">
            <span class="ticker-item"><i class="fa-solid fa-cube"></i>Tekla Structures</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-layer-group"></i>Autodesk Revit</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-road"></i>Civil 3D</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-magnifying-glass-chart"></i>Navisworks</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-cloud"></i>ACC / BIM 360</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-brands fa-python"></i>Python Automation</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-network-wired"></i>Dynamo</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-chart-gantt"></i>4D / 5D Planning</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-hard-hat"></i>Construction Control</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-database"></i>LOD 300–400</span>
            <span class="ticker-sep">◆</span>
            <!-- duplicate for seamless loop -->
            <span class="ticker-item"><i class="fa-solid fa-cube"></i>Tekla Structures</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-layer-group"></i>Autodesk Revit</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-road"></i>Civil 3D</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-magnifying-glass-chart"></i>Navisworks</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-cloud"></i>ACC / BIM 360</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-brands fa-python"></i>Python Automation</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-network-wired"></i>Dynamo</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-chart-gantt"></i>4D / 5D Planning</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-hard-hat"></i>Construction Control</span>
            <span class="ticker-sep">◆</span>
            <span class="ticker-item"><i class="fa-solid fa-database"></i>LOD 300–400</span>
            <span class="ticker-sep">◆</span>
        </div>
    </div>

    <main>

        <!-- ══════════════ ABOUT ══════════════ -->
        <section id="about">
            <div class="container">
                <div class="s-hdr reveal">
                    <div class="s-kicker">About</div>
                    <div class="s-divider"></div>
                    <h2 class="s-title">Kỹ sư BIM<br>Quản lý thi công</h2>
                    <p class="s-desc">Kết hợp chuyên sâu giữa mô hình hóa kỹ thuật số và kinh nghiệm thực chiến tại các
                        dự án hạ tầng giao thông.</p>
                </div>
                <div class="about-grid">
                    <div class="card card-p reveal">
                        <div class="about-banner">
                            <img src="images/cau_vuot_ai.jpg" alt="BIM Coordination Environment" loading="lazy" />
                            <div class="about-banner-label">// BIM Coordination Environment</div>
                        </div>
                        <div class="about-body">
                            <p>Trong gần 03 năm làm việc ở vị trí Kỹ sư xây dựng, tôi đã trực tiếp xử lý bản vẽ, bóc
                                tách khối lượng và quản lý tổ đội nhân công ở các dự án lớn, trọng điểm quốc gia. Với
                                những kinh nghiệm thực tế đã tích lũy được, tôi mong muốn liên tục tiếp cận và làm chủ
                                công nghệ BIM phục vụ quản lý dự án xây dựng quy mô lớn.</p>
                            <p>Thế mạnh cốt lõi của tôi là vai trò <strong class="hl-w">Kỹ sư R&amp;D BIM (Tekla &amp;
                                    Revit)</strong> và kiểm soát hiện trường — trực tiếp xây dựng mô hình đạt cấp độ chi
                                tiết <strong class="hl-w">LOD 300–400</strong>, phối hợp phát hiện xung đột và tích hợp
                                tiến độ <strong class="hl-o">4D/5D</strong>.</p>
                            <p>Tôi tập trung nghiên cứu <strong class="hl">BIM Automation</strong> bằng Python, Dynamo
                                và Grasshopper để loại bỏ các tác vụ lặp, tự động hóa quy trình bóc tách khối lượng và
                                tối ưu hóa phương án biện pháp thi công tại công trường.</p>

                            <div style="margin-top: 24px; padding-top: 20px; border-top: 1px solid var(--border);">
                                <h4
                                    style="font-family: var(--f-head) !important; font-size: 15px; color: var(--text-hi); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 12px; display: flex; align-items: center; gap: 8px;">
                                    <i class="fa-solid fa-graduation-cap" style="color: var(--primary);"></i> Học vấn hệ
                                    chính quy
                                </h4>
                                <div
                                    style="background: rgba(0, 194, 255, 0.03); border: 1px solid var(--border); padding: 14px 18px; border-radius: var(--r-md);">
                                    <div
                                        style="display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; gap: 8px; margin-bottom: 4px;">
                                        <strong style="color: var(--text-hi); font-size: 14.5px;">KỸ THUẬT XÂY DỰNG CÔNG
                                            TRÌNH THỦY (HỆ KỸ SƯ)</strong>
                                        <span
                                            style="font-family: var(--f-mono) !important; font-size: 11px; color: var(--accent);">2018
                                            - 2023</span>
                                    </div>
                                    <div
                                        style="color: var(--primary); font-size: 13px; font-weight: 600; margin-bottom: 6px;">
                                        Trường Đại học Xây dựng Hà Nội (HUCE)</div>
                                    <p style="font-size: 13.5px; color: var(--text-md); line-height: 1.5; margin: 0;">
                                        Đào tạo chuyên sâu về kết cấu công trình thủy lợi, cảng - đường thủy, cơ học đất
                                        và kỹ thuật nền móng. Tốt nghiệp chính thức ngày 02/03/2023.</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="about-specs">
                        <div class="spec-card reveal">
                            <div class="spec-icon"><i class="fa-solid fa-road"></i></div>
                            <div class="spec-title">BIM Hạ tầng giao thông</div>
                            <p class="spec-desc">Mô hình hóa cầu đường, hạ tầng kỹ thuật, bình đồ – trắc dọc – trắc
                                ngang chi tiết.</p>
                        </div>
                        <div class="spec-card reveal">
                            <div class="spec-icon"><i class="fa-solid fa-hard-hat"></i></div>
                            <div class="spec-title">Quản lý thi công</div>
                            <p class="spec-desc">Kiểm soát khối lượng, tiến độ, phối hợp kỹ thuật và xử lý phát sinh tại
                                công trường.</p>
                        </div>
                        <div class="spec-card reveal">
                            <div class="spec-icon"><i class="fa-brands fa-python"></i></div>
                            <div class="spec-title">BIM Automation</div>
                            <p class="spec-desc">Script Python &amp; Dynamo tự động hóa bóc tách, kiểm tra và chuẩn hóa
                                dữ liệu mô hình.</p>
                        </div>
                        <div class="spec-card reveal">
                            <div class="spec-icon"><i class="fa-solid fa-chart-gantt"></i></div>
                            <div class="spec-title">4D / 5D Planning</div>
                            <p class="spec-desc">Tích hợp tiến độ, khối lượng vào mô hình — theo dõi và tối ưu kế hoạch
                                thi công.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- ══════════════ GALLERY ══════════════ -->
        <div class="gallery">
            <div class="container">
                <div class="gallery-grid">
                    <div class="gal-card tall reveal">
                        <img src="images/model-tekla-xe-duc.jpg" alt="Mo hinh xe duc hang Tekla" loading="lazy" />
                        <div class="gal-overlay"></div>
                        <div class="gal-info">
                            <div class="gal-tag"><i class="fa-solid fa-cube"></i>&nbsp;BIM Model</div>
                            <div class="gal-title">Mô hình kết cấu LOD 350+</div>
                            <div class="gal-sub">Tekla Structures · Chi tiết kỹ thuật · Bóc tách KL</div>
                        </div>
                    </div>
                    <div class="gal-right">
                        <div class="gal-card short reveal">
                            <img src="images/cau_vuot_ai.jpg" alt="Thi công cầu vượt" loading="lazy" />
                            <div class="gal-overlay"></div>
                            <div class="gal-info">
                                <div class="gal-tag"><i class="fa-solid fa-hard-hat"></i>&nbsp;Hiện trường</div>
                                <div class="gal-title">Thi công cầu vượt</div>
                                <div class="gal-sub">Giám sát kỹ thuật · Kiểm soát chất lượng</div>
                            </div>
                        </div>
                        <div class="gal-card short reveal">
                            <img src="images/cao_toc_ai.jpg" alt="Đường cao tốc BIM" loading="lazy" />
                            <div class="gal-overlay"></div>
                            <div class="gal-info">
                                <div class="gal-tag"><i class="fa-solid fa-road"></i>&nbsp;Hạ tầng</div>
                                <div class="gal-title">Đường cao tốc BIM</div>
                                <div class="gal-sub">Civil 3D · 4D Planning · Đa bộ môn</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- ══════════════ TOOLS ══════════════ -->
        <section id="tools">
            <div class="container">
                <div class="s-hdr reveal">
                    <div class="s-kicker">Capabilities</div>
                    <div class="s-divider"></div>
                    <h2 class="s-title">Bộ công cụ BIM<br>chuyên nghiệp</h2>
                    <p class="s-desc">Thành thạo toàn bộ chuỗi công cụ từ mô hình hóa, phối hợp kỹ thuật, quản lý dữ
                        liệu đến tự động hóa quy trình BIM.</p>
                </div>
                <div class="tools-grid">
                    <div class="tool-card reveal">
                        <div class="tool-num">// 01</div>
                        <div class="tool-icon-wrap ti-blue"><i class="fa-solid fa-cube"></i></div>
                        <h3>Mô hình kết cấu &amp; hạ tầng</h3>
                        <p>Xây dựng mô hình LOD 300–400 phục vụ thi công, bóc tách và kiểm tra kỹ thuật chi tiết.</p>
                        <div class="tool-tags">
                            <span class="t-tag">Tekla Structures</span>
                            <span class="t-tag">Revit</span>
                            <span class="t-tag">Civil 3D</span>
                        </div>
                    </div>
                    <div class="tool-card reveal">
                        <div class="tool-num">// 02</div>
                        <div class="tool-icon-wrap ti-orange"><i class="fa-solid fa-magnifying-glass-chart"></i></div>
                        <h3>Phối hợp &amp; Clash Detection</h3>
                        <p>Tích hợp đa bộ môn, phát hiện và xử lý xung đột kỹ thuật trước khi ra hiện trường.</p>
                        <div class="tool-tags">
                            <span class="t-tag">Navisworks</span>
                            <span class="t-tag">Clash Detection</span>
                            <span class="t-tag">BIM Coord.</span>
                        </div>
                    </div>
                    <div class="tool-card reveal">
                        <div class="tool-num">// 03</div>
                        <div class="tool-icon-wrap ti-green"><i class="fa-solid fa-chart-gantt"></i></div>
                        <h3>4D / 5D Thi công</h3>
                        <p>Kết nối tiến độ, khối lượng vào mô hình BIM — mô phỏng và tối ưu kế hoạch thi công.</p>
                        <div class="tool-tags">
                            <span class="t-tag">Fuzor</span>
                            <span class="t-tag">4D Schedule</span>
                            <span class="t-tag">5D Quantity</span>
                        </div>
                    </div>
                    <div class="tool-card reveal">
                        <div class="tool-num">// 04</div>
                        <div class="tool-icon-wrap ti-blue"><i class="fa-solid fa-cloud"></i></div>
                        <h3>CDE — Môi trường dữ liệu chung</h3>
                        <p>Quản trị tài liệu, mô hình và luồng phối hợp trên cloud giữa hiện trường và thiết kế.</p>
                        <div class="tool-tags">
                            <span class="t-tag">ACC</span>
                            <span class="t-tag">BIM 360</span>
                            <span class="t-tag">Trimble Connect</span>
                        </div>
                    </div>
                    <div class="tool-card reveal">
                        <div class="tool-num">// 05</div>
                        <div class="tool-icon-wrap ti-yellow"><i class="fa-brands fa-python"></i></div>
                        <h3>BIM Automation</h3>
                        <p>Tự động hóa bóc tách khối lượng, kiểm tra logic mô hình và chuẩn hóa dữ liệu đầu ra.</p>
                        <div class="tool-tags">
                            <span class="t-tag">Python</span>
                            <span class="t-tag">Dynamo</span>
                            <span class="t-tag">API</span>
                        </div>
                    </div>
                    <div class="tool-card reveal">
                        <div class="tool-num">// 06</div>
                        <div class="tool-icon-wrap ti-orange"><i class="fa-solid fa-hard-hat"></i></div>
                        <h3>Kỹ thuật &amp; Quản lý thi công</h3>
                        <p>Kiểm soát kỹ thuật hiện trường, quản lý biện pháp thi công và xử lý phát sinh dự án.</p>
                        <div class="tool-tags">
                            <span class="t-tag">Field Eng.</span>
                            <span class="t-tag">Const. Control</span>
                            <span class="t-tag">QA/QC</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- ══════════════ SKILLS ══════════════ -->
        <section id="skills">
            <div class="container">
                <div class="s-hdr reveal">
                    <div class="s-kicker">Skills</div>
                    <div class="s-divider"></div>
                    <h2 class="s-title">Kỹ năng chuyên môn</h2>
                    <p class="s-desc">Đánh giá năng lực trên các nhóm công cụ và kỹ năng cốt lõi trong quy trình BIM và
                        thi công.</p>
                </div>
                <div class="skills-grid">
                    <div class="card skill-card reveal">
                        <div class="skill-top">
                            <div class="skill-ico"><i class="fa-solid fa-cube"></i></div>
                            <div class="skill-meta">
                                <div class="skill-name">Revit · Civil 3D · Tekla</div>
                            </div>
                            <div class="skill-pct">90%</div>
                        </div>
                        <div class="pbar">
                            <div class="pfill" data-p="90"></div>
                        </div>
                        <p class="skill-note">Làm chủ mô hình hóa kết cấu, hạ tầng và tổ chức dữ liệu mô hình theo hướng
                            triển khai thực tế.</p>
                    </div>
                    <div class="card skill-card reveal">
                        <div class="skill-top">
                            <div class="skill-ico"><i class="fa-solid fa-magnifying-glass-chart"></i></div>
                            <div class="skill-meta">
                                <div class="skill-name">Navisworks · Clash Detection</div>
                            </div>
                            <div class="skill-pct">85%</div>
                        </div>
                        <div class="pbar">
                            <div class="pfill" data-p="85"></div>
                        </div>
                        <p class="skill-note">Phối hợp liên bộ môn, phát hiện xung đột kỹ thuật và hỗ trợ điều phối tại
                            công trường.</p>
                    </div>
                    <div class="card skill-card reveal">
                        <div class="skill-top">
                            <div class="skill-ico"><i class="fa-solid fa-chart-gantt"></i></div>
                            <div class="skill-meta">
                                <div class="skill-name">4D / 5D · Fuzor</div>
                            </div>
                            <div class="skill-pct">80%</div>
                        </div>
                        <div class="pbar">
                            <div class="pfill" data-p="80"></div>
                        </div>
                        <p class="skill-note">Kết nối tiến độ, hình ảnh mô phỏng thi công và dữ liệu quản lý theo mốc
                            triển khai.</p>
                    </div>
                    <div class="card skill-card reveal">
                        <div class="skill-top">
                            <div class="skill-ico"><i class="fa-solid fa-cloud"></i></div>
                            <div class="skill-meta">
                                <div class="skill-name">CDE: ACC / BIM 360 / Trimble</div>
                            </div>
                            <div class="skill-pct">85%</div>
                        </div>
                        <div class="pbar">
                            <div class="pfill" data-p="85"></div>
                        </div>
                        <p class="skill-note">Quản trị dữ liệu chung, kiểm soát tài liệu và luồng phối hợp giữa hiện
                            trường và thiết kế.</p>
                    </div>
                    <div class="card skill-card reveal">
                        <div class="skill-top">
                            <div class="skill-ico"><i class="fa-brands fa-python"></i></div>
                            <div class="skill-meta">
                                <div class="skill-name">Python &amp; Dynamo Automation</div>
                            </div>
                            <div class="skill-pct">78%</div>
                        </div>
                        <div class="pbar">
                            <div class="pfill" data-p="78"></div>
                        </div>
                        <p class="skill-note">Tự động hóa bóc tách, chuẩn hóa dữ liệu, kiểm tra logic mô hình và tăng
                            tốc tác vụ lặp.</p>
                    </div>
                    <div class="card skill-card reveal">
                        <div class="skill-top">
                            <div class="skill-ico"><i class="fa-solid fa-hard-hat"></i></div>
                            <div class="skill-meta">
                                <div class="skill-name">Field Engineering &amp; Construction</div>
                            </div>
                            <div class="skill-pct">88%</div>
                        </div>
                        <div class="pbar">
                            <div class="pfill" data-p="88"></div>
                        </div>
                        <p class="skill-note">Kết nối mô hình BIM với tổ chức thi công, kiểm soát khối lượng và xử lý
                            phát sinh hiện trường.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- ══════════════ EXPERIENCE ══════════════ -->
        <section id="experience">
            <div class="container">
                <div class="s-hdr reveal">
                    <div class="s-kicker">Experience</div>
                    <div class="s-divider"></div>
                    <h2 class="s-title">Kinh nghiệm<br>làm việc</h2>
                    <p class="s-desc">Lộ trình sự nghiệp gắn với BIM triển khai, kỹ thuật hiện trường và ứng dụng mô
                        hình số vào thực tế thi công.</p>
                </div>
                <div class="exp-wrap">
                    <div class="card exp-card reveal">
                        <div class="exp-meta">
                            <div class="exp-period"><i class="fa-solid fa-circle" style="font-size:8px"></i>03/2026 –
                                Hiện tại</div>
                            <div class="exp-current"><span class="status-dot"></span>Active</div>
                        </div>
                        <div class="exp-title">Kỹ sư BIM quản lý xây dựng</div>
                        <div class="exp-company"><i class="fa-solid fa-building"></i>&nbsp;&nbsp;Công ty Cổ phần TVXD
                            A2Z (Tập đoàn Đèo Cả)</div>
                        <p class="exp-desc" style="margin-bottom: 12px; font-weight: 600; color: var(--text-hi);">Xây
                            dựng hệ thống và phát triển giải pháp quản trị số cho dự án hạ tầng:</p>
                        <ul
                            style="margin-left: 20px; margin-bottom: 16px; font-size: 15px; color: #9ab5cc; line-height: 1.8;">
                            <li>Chịu trách nhiệm nghiên cứu, thiết kế và phát triển nền tảng ứng dụng (app) quản lý thi
                                công kỹ thuật số phục vụ công tác điều phối, kiểm soát tiến độ và bóc tách dữ liệu hiện
                                trường.</li>
                            <li>Triển khai thực tế cho dự án trọng điểm quốc gia: Dự án đầu tư xây dựng công trình mở
                                rộng đường cao tốc TP. Hồ Chí Minh - Trung Lương - Mỹ Thuận.</li>
                        </ul>
                        <div class="exp-chips">
                            <span class="echip">BIM Manager</span>
                            <span class="echip">BIM App Platform</span>
                            <span class="echip">Cao tốc HCM-TL-MT</span>
                            <span class="echip">Tập đoàn Đèo Cả</span>
                            <span class="echip">Digital Construction</span>
                        </div>
                    </div>
                    <div class="card exp-card reveal">
                        <div class="exp-meta">
                            <div class="exp-period"><i class="fa-regular fa-circle" style="font-size:8px"></i>08/2023 –
                                03/2026</div>
                        </div>
                        <div class="exp-title">Kỹ thuật hiện trường / Kỹ sư thiết kế</div>
                        <div class="exp-company"><i class="fa-solid fa-building"></i>&nbsp;&nbsp;Công ty Cổ phần Đầu tư
                            và Xây dựng giao thông Phương Thành</div>
                        <p class="exp-desc" style="margin-bottom: 12px; font-weight: 600; color: var(--text-hi);">Trực
                            tiếp điều động và tham gia các công trình giao thông trọng điểm quốc gia:</p>
                        <ul
                            style="margin-left: 20px; margin-bottom: 16px; font-size: 15px; color: #9ab5cc; line-height: 1.8;">
                            <li><b>Tháng 08/2023 đến 05/2025:</b> Trực tiếp tham gia thi công cụm cầu Quảng Sơn thuộc
                                gói thầu XL-02 thuộc dự án thành phần đoạn Vũng Áng - Bùng tuyến cao tốc Bắc - Nam. Chịu
                                trách nhiệm bóc tách khối lượng hiện trường, quản lý tổ đội nhân công, phối hợp thi công
                                kết cấu nhịp và hạ bộ cầu.</li>
                            <li><b>Tháng 05/2025 đến 03/2026:</b> Tham gia thiết kế bản vẽ thi công (TKBVTC) cho các dự
                                án
                                trọng điểm quốc gia như Sân Bay Long Thành, Cầu Hải Thành, Cầu Tứ Liên kết hợp lập báo
                                cáo tiến độ, sản lượng và lập kế hoạch thi công.</li>
                        </ul>
                        <div class="exp-chips">
                            <span class="echip">Field Engineering</span>
                            <span class="echip">Quantity Surveying</span>
                            <span class="echip">BIM for Bridges</span>
                            <span class="echip">Cao tốc Bắc - Nam</span>
                            <span class="echip">Sân bay Long Thành</span>
                        </div>
                    </div>
                    <div class="card exp-card reveal">
                        <div class="exp-meta">
                            <div class="exp-period"><i class="fa-regular fa-circle" style="font-size:8px"></i>11/2022 –
                                07/2023</div>
                        </div>
                        <div class="exp-title">Kỹ sư tư vấn thiết kế</div>
                        <div class="exp-company"><i class="fa-solid fa-building"></i>&nbsp;&nbsp;TEDI-WECCO (Công ty CP
                            Tư vấn Thiết kế Xây dựng Giao thông thủy)</div>
                        <ul
                            style="margin-left: 20px; margin-bottom: 16px; font-size: 15px; color: #9ab5cc; line-height: 1.8;">
                            <li><b>Tháng 11/2022 đến 03/2023:</b> Thực tập làm việc tại công ty, nghiên cứu quy trình
                                thiết kế công trình cảng - đường thủy.</li>
                            <li><b>Tháng 04/2023 đến 06/2023:</b> Làm tư vấn thiết kế tại phòng kỹ thuật hạ tầng, triển
                                khai bản vẽ hình học và kết cấu cảng.</li>
                            <li><b>Tháng 06/2023 đến 07/2023:</b> Hỗ trợ tư vấn giám sát hiện trường tại công trình Kè
                                Tam Bạc, thành phố Hải Phòng.</li>
                        </ul>
                        <div class="exp-chips">
                            <span class="echip">TEDI-WECCO</span>
                            <span class="echip">Cảng &amp; Đường thủy</span>
                            <span class="echip">Hạ tầng kỹ thuật</span>
                            <span class="echip">Kè Tam Bạc</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- ══════════════ PROJECTS / 3D MODELS HUB ══════════════ -->
        <section id="projects">
            <div class="container">
                <div class="s-hdr reveal">
                    <div class="s-kicker">Digital Twin &amp; 3D Models</div>
                    <div class="s-divider"></div>
                    <h2 class="s-title">Kho mô hình 3D &amp; Dự án</h2>
                    <p class="s-desc">Bộ sưu tập mô hình kỹ thuật số chuyên sâu về biện pháp thi công, tự động hóa và
                        quản lý tiến độ do tôi trực tiếp phát triển.</p>
                </div>

                <!-- Tab Filter Navigation -->
                <div class="tab-container reveal">
                    <div class="tab-nav">
                        <button class="tab-btn active" data-tab="tab-tekla">Tekla &amp; Grasshopper</button>
                        <button class="tab-btn" data-tab="tab-revit">Revit &amp; BIM Thi Công</button>
                        <button class="tab-btn" data-tab="tab-dynamo">Revit Cầu &amp; Dynamo</button>
                        <button class="tab-btn" data-tab="tab-planning">Planning &amp; Control</button>
                    </div>

                    <!-- Tab 1: Tekla -->
                    <div class="tab-content active" id="tab-tekla">
                        <div class="proj-grid">
                            <article class="card proj-card zoomable" data-src="images/model-tekla-nha-xuong.jpg"
                                title="Mô hình nhà thép và nhà xưởng công nghiệp">
                                <div class="proj-media">
                                    <img src="images/model-tekla-nha-xuong.jpg" alt="Mo hinh nha thep" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Tekla Structure</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Steel Frame</span>
                                        <span class="ptag">LOD 400</span>
                                    </div>
                                    <h3 class="proj-title">Nhà thép &amp; Nhà xưởng công nghiệp</h3>
                                    <p class="proj-desc">Mô hình hóa kết cấu thép nhà nhiều tầng và nhà xưởng khẩu độ
                                        lớn. Triển khai cấu kiện chi tiết phục vụ gia công chế tạo và bóc tách khối
                                        lượng.</p>
                                    <span class="proj-link">Xem chi tiết mô hình &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>

                            <article class="card proj-card zoomable" data-src="images/model-tekla-da-giao.jpg"
                                title="Hệ biện pháp đà giáo thi công cầu">
                                <div class="proj-media">
                                    <img src="images/model-tekla-da-giao.jpg" alt="Da giao thi cong" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Bridge Method</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Trụ P4</span>
                                        <span class="ptag">Xe đúc K</span>
                                        <span class="ptag">Đà giáo</span>
                                    </div>
                                    <h3 class="proj-title">Biện pháp đà giáo &amp; Xe đúc hẫng</h3>
                                    <p class="proj-desc">Dựng mô hình 3D đà giáo thi công trụ P4, đà giáo đỡ nhịp và kết
                                        cấu xe đúc hẫng cân bằng (xe đúc K) phục vụ lập biện pháp hiện trường.</p>
                                    <span class="proj-link">Xem chi tiết mô hình &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>

                            <article class="card proj-card zoomable" data-src="images/model-grasshopper-tekla.jpg"
                                title="Nghiên cứu tool Grasshopper Tekla">
                                <div class="proj-media">
                                    <img src="images/model-grasshopper-tekla.jpg" alt="Grasshopper Tekla Tool"
                                        loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// BIM Automation</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Grasshopper</span>
                                        <span class="ptag">Tekla API</span>
                                        <span class="ptag">3D Rebar</span>
                                    </div>
                                    <h3 class="proj-title">Thuật toán tự động Tekla</h3>
                                    <p class="proj-desc">Phát triển thuật toán rải cốt thép 3 chiều tự động trong Tekla
                                        thông qua Grasshopper và thuật toán xếp bê tông ngẫu nhiên tự động.</p>
                                    <span class="proj-link">Xem chi tiết nghiên cứu &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>
                        </div>
                    </div>

                    <!-- Tab 2: Revit -->
                    <div class="tab-content" id="tab-revit">
                        <div class="proj-grid">
                            <article class="card proj-card zoomable" data-src="images/model-revit-dan-dung.jpg"
                                title="Revit công trình dân dụng & Nhà biệt thự">
                                <div class="proj-media">
                                    <img src="images/model-revit-dan-dung.jpg" alt="Revit dan dung" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Revit Architecture</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Biệt thự Tân Cổ</span>
                                        <span class="ptag">Hải Hậu</span>
                                        <span class="ptag">Tòa nhà VP</span>
                                    </div>
                                    <h3 class="proj-title">Revit Dân Dụng &amp; Tòa Nhà Văn Phòng</h3>
                                    <p class="proj-desc">Thiết kế mô hình kết cấu và kiến trúc biệt thự Tân Cổ Điển tại
                                        Hải Hậu và mô hình đồ án tòa nhà văn phòng cho thuê quy mô lớn.</p>
                                    <span class="proj-link">Xem chi tiết mô hình &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>

                            <article class="card proj-card zoomable" data-src="images/model-revit-cang-thuy.jpg"
                                title="Mô hình Revit Cảng đường thủy tốt nghiệp">
                                <div class="proj-media">
                                    <img src="images/model-revit-cang-thuy.jpg" alt="Revit cang thuy" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Port structure</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Đồ án tốt nghiệp</span>
                                        <span class="ptag">Bến tàu</span>
                                        <span class="ptag">Hiền Trang</span>
                                    </div>
                                    <h3 class="proj-title">Revit Cảng &amp; Công Trình Đường Thủy</h3>
                                    <p class="proj-desc">Mô hình hóa chi tiết 3D mặt cắt ngang điển hình và kết cấu bến
                                        tàu nhỏ Hiền Trang phục vụ Đồ án tốt nghiệp Kỹ sư công trình thủy.</p>
                                    <span class="proj-link">Xem chi tiết mô hình &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>

                            <article class="card proj-card zoomable" data-src="images/model-revit-thi-cong.jpg"
                                title="Family biện pháp thi công tự tạo trong Revit">
                                <div class="proj-media">
                                    <img src="images/model-revit-thi-cong.jpg" alt="Revit thi cong" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Revit Family</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Hệ HIROSE</span>
                                        <span class="ptag">Văng chống</span>
                                        <span class="ptag">Bao che</span>
                                    </div>
                                    <h3 class="proj-title">Thư viện Family Biện Pháp Thi Công</h3>
                                    <p class="proj-desc">Tự thiết kế các cấu kiện Family phục vụ mô phỏng biện pháp thi
                                        công: Hệ HIROSE điển hình, hệ văng chống hố móng sâu, model bao che cao tầng.
                                    </p>
                                    <span class="proj-link">Xem chi tiết thư viện &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>
                        </div>
                    </div>

                    <!-- Tab 3: Dynamo -->
                    <div class="tab-content" id="tab-dynamo">
                        <div class="proj-grid">
                            <article class="card proj-card zoomable" data-src="images/model-dynamo-dam-ban.jpg"
                                title="Mô phỏng dầm bản rỗng và cầu Tân An bằng Dynamo">
                                <div class="proj-media">
                                    <img src="images/model-dynamo-dam-ban.jpg" alt="Dynamo dam ban" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Parametric Design</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Cầu Tân An</span>
                                        <span class="ptag">Dầm bản rỗng</span>
                                        <span class="ptag">Dynamo API</span>
                                    </div>
                                    <h3 class="proj-title">Dynamo Dầm Bản Rỗng &amp; Cầu Tân An</h3>
                                    <p class="proj-desc">Lập trình tham số hóa mô hình kết cấu dầm bản rỗng và cấu trúc
                                        cầu Tân An bằng Dynamo, giúp tự động sinh mô hình theo thông số hình học.</p>
                                    <span class="proj-link">Xem chi tiết thuật toán &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>

                            <article class="card proj-card zoomable" data-src="images/model-dynamo-super-t.jpg"
                                title="Tự động bố trí thép dầm Super T và cống hộp bằng Dynamo">
                                <div class="proj-media">
                                    <img src="images/model-dynamo-super-t.jpg" alt="Dynamo Super T" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Excel Integration</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Dầm Super T</span>
                                        <span class="ptag">Cốt thép cống</span>
                                        <span class="ptag">Excel Link</span>
                                    </div>
                                    <h3 class="proj-title">Auto Model Dầm Super T &amp; Cốt Thép</h3>
                                    <p class="proj-desc">Xây dựng script Dynamo tự động import thông số từ file Excel để
                                        dựng dầm Super T, tự động bố trí cốt thép dầm và cốt thép cống hộp.</p>
                                    <span class="proj-link">Xem chi tiết thuật toán &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>
                        </div>
                    </div>

                    <!-- Tab 4: Planning -->
                    <div class="tab-content" id="tab-planning">
                        <div class="proj-grid">
                            <article class="card proj-card zoomable" data-src="images/model-planning-4d.jpg"
                                title="Mô phỏng biện pháp thi công và kiểm tra xung đột">
                                <div class="proj-media">
                                    <img src="images/model-planning-4d.jpg" alt="Mo phong 4D" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// BIM Coordination</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Fuzor 4D</span>
                                        <span class="ptag">Infraworks</span>
                                        <span class="ptag">Navisworks</span>
                                    </div>
                                    <h3 class="proj-title">Mô phỏng 4D &amp; Điều Phối Đa Bộ Môn</h3>
                                    <p class="proj-desc">Tích hợp mô phỏng biện pháp thi công và tiến độ trên Fuzor,
                                        dựng bình đồ hạ tầng giao thông trên Infraworks và kiểm tra xung đột Navisworks.
                                    </p>
                                    <span class="proj-link">Xem chi tiết mô phỏng &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>

                            <article class="card proj-card zoomable" data-src="images/model-planning-p6.jpg"
                                title="Lập tiến độ thi công tổng thể dự án">
                                <div class="proj-media">
                                    <img src="images/model-planning-p6.jpg" alt="Lap tien do thi cong" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Project Scheduling</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">Primavera P6</span>
                                        <span class="ptag">MS Project</span>
                                        <span class="ptag">Gantt Chart</span>
                                    </div>
                                    <h3 class="proj-title">Tiến Độ Thi Công P6 &amp; MS Project</h3>
                                    <p class="proj-desc">Lập bảng tiến độ thi công, quản lý đường găng dự án (CPM),
                                        thiết lập biểu đồ điều phối nhân công và phân bổ thiết bị thi công dự án.</p>
                                    <span class="proj-link">Xem chi tiết tiến độ &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>

                            <article class="card proj-card zoomable" data-src="images/model-planning-evm.jpg"
                                title="Báo cáo quản lý sản lượng và phân tích giá trị thu được (EVM)">
                                <div class="proj-media">
                                    <img src="images/model-planning-evm.jpg" alt="Bao cao EVM" loading="lazy" />
                                    <div class="proj-overlay"></div>
                                    <span class="proj-badge">// Project Controls</span>
                                </div>
                                <div class="proj-body">
                                    <div class="proj-tags">
                                        <span class="ptag">EVM Analysis</span>
                                        <span class="ptag">Biểu đồ CP/DT</span>
                                        <span class="ptag">Dashboard</span>
                                    </div>
                                    <h3 class="proj-title">Báo Cáo Sản Lượng &amp; Phân Tích EVM</h3>
                                    <p class="proj-desc">Thiết lập dashboard phân tích các chỉ số quản lý dự án (CV, SV,
                                        CPI, SPI), biểu đồ doanh thu - chi phí lũy kế và biểu đồ huy động nhân công/máy.
                                    </p>
                                    <span class="proj-link">Xem báo cáo phân tích &nbsp;<i
                                            class="fa-solid fa-expand"></i></span>
                                </div>
                            </article>
                        </div>
                    </div>

                </div>
            </div>
        </section>

        <!-- ══════════════ CERTS ══════════════ -->
        <section id="certificates">
            <div class="container">
                <div class="s-hdr reveal">
                    <div class="s-kicker">Recognition</div>
                    <div class="s-divider"></div>
                    <h2 class="s-title">Chứng chỉ chuyên môn</h2>
                    <p class="s-desc">Nền tảng học tập tập trung vào mô hình hóa kỹ thuật, dữ liệu và tự động hóa phục
                        vụ công việc thực chiến.</p>
                </div>
                <div class="cert-grid">
                    <!-- Bằng Kỹ sư HUCE -->
                    <div class="card cert-card zoomable" data-src="images/bang-ky-su.jpg"
                        title="Bằng Kỹ sư - Trường Đại học Xây dựng Hà Nội">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-graduation-cap"></i></div>
                            <div>
                                <div class="cert-title">Bằng Kỹ sư Xây dựng</div>
                                <div class="cert-issuer">Trường Đại học Xây dựng Hà Nội (HUCE)</div>
                                <p class="cert-desc">Ngành học: Kỹ thuật xây dựng công trình thủy. Đào tạo chuyên sâu về
                                    kỹ thuật kết cấu công trình, thủy lực, cơ học đất và thiết kế nền móng.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/bang-ky-su.jpg" alt="Bang Ky su HUCE" class="cert-img" loading="lazy" />
                        </div>
                    </div>

                    <!-- Chứng chỉ Revit 2027 A2Z / Autodesk ATC -->
                    <div class="card cert-card zoomable" data-src="images/cert-autodesk-revit2027.jpg"
                        title="Transportation Infrastructure Design with Revit 2027 - Autodesk ATC">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-road"></i></div>
                            <div>
                                <div class="cert-title">Transportation Infrastructure Design with Revit 2027</div>
                                <div class="cert-issuer">Autodesk ATC / A2Z Construction Consulting</div>
                                <p class="cert-desc">Chứng nhận hoàn thành chương trình đào tạo tiêu chuẩn của Autodesk
                                    về thiết kế hạ tầng giao thông đường bộ, cầu cống bằng phần mềm Revit 2027.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/cert-autodesk-revit2027.jpg"
                                alt="Autodesk Revit 2027 Infrastructure Design" class="cert-img" loading="lazy" />
                        </div>
                    </div>

                    <!-- Chứng chỉ Tekla ATC -->
                    <div class="card cert-card zoomable" data-src="images/cert-tekla-atc.jpg"
                        title="Chứng nhận Tekla Structures version 2025 - ATC">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-cube"></i></div>
                            <div>
                                <div class="cert-title">Tekla Structures version 2025 (BIM Bridge)</div>
                                <div class="cert-issuer">ATC Authorized Training Center</div>
                                <p class="cert-desc">Đào tạo chuyên sâu ứng dụng mô hình hóa Tekla Structures trong
                                    thiết kế biện pháp thi công và triển khai cấu kiện cầu giao thông.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/cert-tekla-atc.jpg" alt="ATC Tekla Structures" class="cert-img"
                                loading="lazy" />
                        </div>
                    </div>

                    <!-- Chứng chỉ QS GXD -->
                    <div class="card cert-card zoomable" data-src="images/cert-qs.jpg"
                        title="Chứng chỉ Kỹ sư Quantity Surveyor (QS) - GXD">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-calculator"></i></div>
                            <div>
                                <div class="cert-title">Nghiệp vụ Kỹ sư Quantity Surveyor (QS)</div>
                                <div class="cert-issuer">Tổ chức Giáo dục GXD (gxd.edu.vn)</div>
                                <p class="cert-desc">Chứng chỉ đo bóc khối lượng, lập dự toán công trình, quản lý chi
                                    phí dự án và kiểm soát hao hụt vật tư hiện trường xây dựng.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/cert-qs.jpg" alt="Chung chi QS GXD" class="cert-img" loading="lazy" />
                        </div>
                    </div>

                    <!-- Chứng chỉ BIM ATC HUCE -->
                    <div class="card cert-card zoomable" data-src="images/cert-bim-atc.jpg"
                        title="Chứng nhận BIM Technology and Project Management - ATC">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-project-diagram"></i></div>
                            <div>
                                <div class="cert-title">BIM Technology &amp; Project Management</div>
                                <div class="cert-issuer">HUCE Authorized Training Center (ATC)</div>
                                <p class="cert-desc">Chứng nhận hoàn thành khóa học đào tạo tiêu chuẩn về quy trình công
                                    nghệ BIM, điều phối thông tin và quản lý dự án xây dựng.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/cert-bim-atc.jpg" alt="ATC BIM Technology" class="cert-img"
                                loading="lazy" />
                        </div>
                    </div>

                    <!-- Google Gemini Certified Educator -->
                    <div class="card cert-card zoomable" data-src="images/cert-google-gemini.jpg"
                        title="Google Gemini Certified Educator - Google for Education">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-brain"></i></div>
                            <div>
                                <div class="cert-title">Gemini Certified Educator</div>
                                <div class="cert-issuer">Google for Education</div>
                                <p class="cert-desc">Chứng nhận năng lực ứng dụng công nghệ trí tuệ nhân tạo (Generative
                                    AI - Google Gemini) trong công tác giảng dạy, truyền đạt thông tin và nghiên cứu
                                    giáo dục.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/cert-google-gemini.jpg" alt="Google Gemini Certified Educator"
                                class="cert-img" loading="lazy" />
                        </div>
                    </div>

                    <!-- Google Data Analytics: Ask Questions -->
                    <div class="card cert-card zoomable" data-src="images/cert-google-question.jpg"
                        title="Ask Questions to Make Data-Driven Decisions - Google & Coursera">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-chart-line"></i></div>
                            <div>
                                <div class="cert-title">Ask Questions to Make Data-Driven Decisions</div>
                                <div class="cert-issuer">Google &amp; Coursera</div>
                                <p class="cert-desc">Khóa học chuyên sâu thuộc chương trình Google Data Analytics
                                    Professional Certificate, tập trung vào phương pháp đặt câu hỏi và lập kế hoạch phân
                                    tích dựa trên dữ liệu.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/cert-google-question.jpg" alt="Google Data Analytics Ask Questions"
                                class="cert-img" loading="lazy" />
                        </div>
                    </div>

                    <!-- Google Data Analytics: Prepare Data -->
                    <div class="card cert-card zoomable" data-src="images/cert-google-explore.jpg"
                        title="Prepare Data for Exploration - Google & Coursera">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-database"></i></div>
                            <div>
                                <div class="cert-title">Prepare Data for Exploration</div>
                                <div class="cert-issuer">Google &amp; Coursera</div>
                                <p class="cert-desc">Khóa học phân tích chuyên sâu về các phương pháp thu thập, định
                                    dạng, quản lý và tổ chức dữ liệu phục vụ nghiên cứu và trực quan hóa.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/cert-google-explore.jpg" alt="Google Data Analytics Prepare Data"
                                class="cert-img" loading="lazy" />
                        </div>
                    </div>

                    <!-- Google Data Analytics: Process Data -->
                    <div class="card cert-card zoomable" data-src="images/cert-google-clean.jpg"
                        title="Process Data from Dirty to Clean - Google & Coursera">
                        <div class="cert-head">
                            <div class="cert-ico"><i class="fa-solid fa-broom"></i></div>
                            <div>
                                <div class="cert-title">Process Data from Dirty to Clean</div>
                                <div class="cert-issuer">Google &amp; Coursera</div>
                                <p class="cert-desc">Khóa học chuyên sâu về quy trình xử lý dữ liệu thô, loại bỏ sai
                                    sót, lọc dữ liệu lỗi và chuẩn hóa dữ liệu đầu ra phục vụ phân tích dự án.</p>
                            </div>
                        </div>
                        <div class="cert-img-wrap">
                            <img src="images/cert-google-clean.jpg" alt="Google Data Analytics Process Data"
                                class="cert-img" loading="lazy" />
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- ══════════════ CONTACT ══════════════ -->
        <section id="contact">
            <div class="container">
                <div class="s-hdr reveal">
                    <div class="s-kicker">Connect</div>
                    <div class="s-divider"></div>
                    <h2 class="s-title">Liên hệ hợp tác</h2>
                    <p class="s-desc">Sẵn sàng trao đổi về BIM triển khai, mô hình hạ tầng, workflow automation hoặc
                        giải pháp quản lý số cho thi công.</p>
                </div>
                <div class="contact-grid">
                    <div class="reveal">
                        <div class="card card-p contact-lead">
                            <p>Nếu bạn đang tìm một kỹ sư có thể kết nối giữa mô hình BIM, dữ liệu kỹ thuật và yêu cầu
                                triển khai thực tế tại công trường, tôi rất sẵn sàng trao đổi để cùng tối ưu quy trình
                                dự án.</p>
                            <div class="contact-list">
                                <div class="contact-row">
                                    <div class="contact-ico"><i class="fa-solid fa-envelope"></i></div>
                                    <div>
                                        <div class="c-lbl">Email</div>
                                        <div class="c-val">vnpphat2710@gmail.com</div>
                                    </div>
                                </div>
                                <div class="contact-row">
                                    <div class="contact-ico"><i class="fa-solid fa-phone"></i></div>
                                    <div>
                                        <div class="c-lbl">Số điện thoại</div>
                                        <div class="c-val">0825.632.226</div>
                                    </div>
                                </div>
                                <div class="contact-row">
                                    <div class="contact-ico"><i class="fa-solid fa-globe"></i></div>
                                    <div>
                                        <div class="c-lbl">Website / Portfolio</div>
                                        <div class="c-val">vnp-kuro.github.io</div>
                                    </div>
                                </div>
                                <div class="contact-row">
                                    <div class="contact-ico"><i class="fa-solid fa-layer-group"></i></div>
                                    <div>
                                        <div class="c-lbl">Lĩnh vực hợp tác</div>
                                        <div class="c-val">BIM · Hạ tầng · Automation · Quản lý thi công</div>
                                    </div>
                                </div>
                            </div>
                            <div class="social-row">
                                <a href="https://vnp-kuro.github.io/" target="_blank" class="soc-btn"
                                    rel="noreferrer"><i class="fa-brands fa-github"></i>GitHub</a>
                                <a href="https://www.facebook.com/phat.vu.31945" target="_blank" class="soc-btn"
                                    rel="noreferrer"><i class="fa-brands fa-facebook-f"></i>Facebook</a>
                                <a href="https://www.youtube.com/@kuro7746" target="_blank" class="soc-btn"
                                    rel="noreferrer"><i class="fa-brands fa-youtube"></i>YouTube</a>
                                <a href="https://www.tiktok.com/@vnpkuro" target="_blank" class="soc-btn"
                                    rel="noreferrer"><i class="fa-brands fa-tiktok"></i>TikTok</a>
                                <a href="https://www.instagram.com/vnpphat/" target="_blank" class="soc-btn"
                                    rel="noreferrer"><i class="fa-brands fa-instagram"></i>Instagram</a>
                            </div>
                        </div>
                    </div>
                    <div class="contact-side reveal">
                        <div class="mini-panel">
                            <div class="mp-ico"><i class="fa-solid fa-lightbulb"></i></div>
                            <h3>Tư duy triển khai</h3>
                            <p>Ưu tiên mô hình đủ sâu để hỗ trợ thi công, kiểm soát dữ liệu chặt chẽ và giảm thao tác
                                thủ công bằng tự động hóa.</p>
                        </div>
                        <div class="mini-panel">
                            <div class="mp-ico"><i class="fa-solid fa-bolt"></i></div>
                            <h3>Phong cách làm việc</h3>
                            <p>Nhanh, trực quan, có logic dữ liệu và bám sát nhu cầu thực tế của dự án thay vì chỉ dừng
                                ở mô hình trình bày.</p>
                        </div>
                        <div class="mini-panel">
                            <div class="mp-ico"><i class="fa-solid fa-rocket"></i></div>
                            <h3>Hướng phát triển</h3>
                            <p>Mở rộng BIM Automation, Data Science trong xây dựng và xây dựng quy trình số hóa phục vụ
                                hiện trường.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

    </main>

    <!-- ══════════════ FOOTER ══════════════ -->
    <footer>
        <div class="container foot-inner">
            <div class="foot-brand">
                <div class="foot-mark"><i class="fa-solid fa-building-columns"></i></div>
                <span class="foot-name">Vũ Ngọc Phát · BIM Engineer</span>
            </div>
            <div class="foot-note">© 2026 · BIM Hạ tầng · Construction Management · Automation</div>
            <div class="foot-note">// Syne · Rajdhani · JetBrains Mono</div>
        </div>
    </footer>

    <script>
        /* ═══════════════════════════════════════════════
           TECH BACKGROUND ENGINE v2
           - Particle network with mouse repulsion
           - Hex grid with cursor proximity highlight
           - Click ripple wave effect
           - Crosshair cursor ring
           - Smooth glow trail
           ═══════════════════════════════════════════════ */

        const cvs = document.getElementById('tech-canvas');
        const cx = cvs.getContext('2d');
        const pp = document.getElementById('profilePanel');
        const rCvs = document.getElementById('ripple-canvas');
        const rCtx = rCvs.getContext('2d');
        const hCvs = document.getElementById('hex-canvas');
        const hCtx = hCvs.getContext('2d');

        const mo = { x: null, y: null, px: null, py: null, vx: 0, vy: 0 };
        const ripples = [];
        const pts = [];
        const HEX_SIZE = 46;

        /* ── Resize all canvases ── */
        function resize() {
            const dpr = window.devicePixelRatio || 1;
            [cvs, rCvs, hCvs].forEach(function (c) {
                c.width = window.innerWidth * dpr;
                c.height = window.innerHeight * dpr;
                c.style.width = window.innerWidth + 'px';
                c.style.height = window.innerHeight + 'px';
            });
            cx.setTransform(dpr, 0, 0, dpr, 0, 0);
            rCtx.setTransform(dpr, 0, 0, dpr, 0, 0);
            hCtx.setTransform(dpr, 0, 0, dpr, 0, 0);
            initPts();
        }

        /* ── Init particles ── */
        function initPts() {
            pts.length = 0;
            var n = Math.min(120, Math.max(60, Math.floor(window.innerWidth / 14)));
            for (var i = 0; i < n; i++) {
                pts.push({
                    x: Math.random() * window.innerWidth,
                    y: Math.random() * window.innerHeight,
                    ox: 0, oy: 0,
                    vx: (Math.random() - 0.5) * 0.45,
                    vy: (Math.random() - 0.5) * 0.45,
                    s: Math.random() * 1.8 + 0.6,
                    brightness: 0.4 + Math.random() * 0.4
                });
            }
        }

        /* ── Draw hex grid with cursor glow ── */
        function drawHex() {
            hCtx.clearRect(0, 0, window.innerWidth, window.innerHeight);
            var cols = Math.ceil(window.innerWidth / (HEX_SIZE * 1.75)) + 2;
            var rows = Math.ceil(window.innerHeight / (HEX_SIZE * 1.53)) + 2;
            for (var row = -1; row < rows; row++) {
                for (var col = -1; col < cols; col++) {
                    var cx0 = col * HEX_SIZE * 1.75 + (row % 2 === 0 ? 0 : HEX_SIZE * 0.875);
                    var cy0 = row * HEX_SIZE * 1.53 * 0.5;
                    var dist = Infinity;
                    if (mo.x !== null) {
                        dist = Math.sqrt((cx0 - mo.x) * (cx0 - mo.x) + (cy0 - mo.y) * (cy0 - mo.y));
                    }
                    var maxD = 200;
                    var t = Math.max(0, 1 - dist / maxD);
                    var alpha = 0.03 + t * 0.28;
                    var r = HEX_SIZE * 0.88;
                    hCtx.beginPath();
                    for (var k = 0; k < 6; k++) {
                        var angle = Math.PI / 180 * (60 * k - 30);
                        var px = cx0 + r * Math.cos(angle);
                        var py = cy0 + r * Math.sin(angle);
                        k === 0 ? hCtx.moveTo(px, py) : hCtx.lineTo(px, py);
                    }
                    hCtx.closePath();
                    if (t > 0.05) {
                        hCtx.fillStyle = 'rgba(0,194,255,' + (t * 0.07) + ')';
                        hCtx.fill();
                    }
                    hCtx.strokeStyle = 'rgba(0,194,255,' + alpha + ')';
                    hCtx.lineWidth = 0.8;
                    hCtx.stroke();
                }
            }
        }

        /* ── Draw particles with repulsion ── */
        function drawParticles() {
            cx.clearRect(0, 0, window.innerWidth, window.innerHeight);
            var REPEL = 120;
            for (var i = 0; i < pts.length; i++) {
                var p = pts[i];
                /* Mouse repulsion */
                if (mo.x !== null) {
                    var dx = p.x - mo.x, dy = p.y - mo.y;
                    var d = Math.sqrt(dx * dx + dy * dy);
                    if (d < REPEL && d > 0) {
                        var force = (REPEL - d) / REPEL;
                        p.vx += (dx / d) * force * 0.55;
                        p.vy += (dy / d) * force * 0.55;
                    }
                }
                /* Friction */
                p.vx *= 0.96; p.vy *= 0.96;
                /* Limit speed */
                var spd = Math.sqrt(p.vx * p.vx + p.vy * p.vy);
                if (spd > 3) { p.vx = p.vx / spd * 3; p.vy = p.vy / spd * 3; }
                if (spd < 0.25) { p.vx += (Math.random() - 0.5) * 0.05; p.vy += (Math.random() - 0.5) * 0.05; }

                p.x += p.vx; p.y += p.vy;
                if (p.x < 0) p.x = window.innerWidth;
                if (p.x > window.innerWidth) p.x = 0;
                if (p.y < 0) p.y = window.innerHeight;
                if (p.y > window.innerHeight) p.y = 0;

                /* Proximity to mouse → brightness boost */
                var glow = 0.5;
                if (mo.x !== null) {
                    var ddx = p.x - mo.x, ddy = p.y - mo.y;
                    var dd = Math.sqrt(ddx * ddx + ddy * ddy);
                    glow = 0.5 + Math.max(0, 1 - dd / 200) * 0.9;
                }
                cx.beginPath();
                cx.arc(p.x, p.y, p.s * (0.8 + glow * 0.3), 0, Math.PI * 2);
                cx.fillStyle = 'rgba(0,194,255,' + (p.brightness * glow) + ')';
                if (glow > 1) {
                    cx.shadowColor = 'rgba(0,194,255,0.8)';
                    cx.shadowBlur = 8;
                } else {
                    cx.shadowBlur = 0;
                }
                cx.fill();
                cx.shadowBlur = 0;

                /* Connections */
                for (var j = i + 1; j < pts.length; j++) {
                    var q = pts[j];
                    var ex = p.x - q.x, ey = p.y - q.y;
                    var ed = Math.sqrt(ex * ex + ey * ey);
                    if (ed < 120) {
                        cx.beginPath();
                        cx.moveTo(p.x, p.y); cx.lineTo(q.x, q.y);
                        cx.strokeStyle = 'rgba(0,194,255,' + (0.1 - ed / 1400) + ')';
                        cx.lineWidth = 0.7; cx.stroke();
                    }
                }

                /* Cursor connection lines */
                if (mo.x !== null) {
                    var mdx = p.x - mo.x, mdy = p.y - mo.y;
                    var md = Math.sqrt(mdx * mdx + mdy * mdy);
                    if (md < 180) {
                        var a = (1 - md / 180) * 0.45;
                        cx.beginPath();
                        cx.moveTo(p.x, p.y); cx.lineTo(mo.x, mo.y);
                        cx.strokeStyle = 'rgba(0,229,160,' + a + ')';
                        cx.lineWidth = 0.9;
                        cx.stroke();
                    }
                }
            }
        }

        /* ── Ripple system ── */
        function drawRipples() {
            rCtx.clearRect(0, 0, window.innerWidth, window.innerHeight);
            for (var i = ripples.length - 1; i >= 0; i--) {
                var rp = ripples[i];
                rp.r += rp.speed;
                rp.r2 += rp.speed * 1.8;
                rp.life -= 0.018;
                if (rp.life <= 0) { ripples.splice(i, 1); continue; }

                rCtx.beginPath();
                rCtx.arc(rp.x, rp.y, rp.r, 0, Math.PI * 2);
                rCtx.strokeStyle = 'rgba(0,194,255,' + (rp.life * 0.6) + ')';
                rCtx.lineWidth = 1.5;
                rCtx.stroke();

                rCtx.beginPath();
                rCtx.arc(rp.x, rp.y, rp.r2, 0, Math.PI * 2);
                rCtx.strokeStyle = 'rgba(0,229,160,' + (rp.life * 0.3) + ')';
                rCtx.lineWidth = 0.8;
                rCtx.stroke();
            }
        }

        /* ── Main loop ── */
        function loop() {
            drawHex();
            drawParticles();
            drawRipples();
            requestAnimationFrame(loop);
        }

        resize(); loop();
        window.addEventListener('resize', resize);

        /* ── Mouse tracking ── */
        window.addEventListener('mousemove', function (e) {
            mo.px = mo.x; mo.py = mo.y;
            mo.x = e.clientX; mo.y = e.clientY;
            if (mo.px !== null) {
                mo.vx = mo.x - mo.px;
                mo.vy = mo.y - mo.py;
            }
        });
        window.addEventListener('mouseleave', function () {
            mo.x = null; mo.y = null;
        });

        /* ── Click ripple ── */
        window.addEventListener('click', function (e) {
            ripples.push({ x: e.clientX, y: e.clientY, r: 4, r2: 4, speed: 6, life: 1 });
            ripples.push({ x: e.clientX, y: e.clientY, r: 0, r2: 0, speed: 4, life: 0.8 });
            /* Burst: repel nearby particles */
            pts.forEach(function (p) {
                var dx = p.x - e.clientX, dy = p.y - e.clientY;
                var d = Math.sqrt(dx * dx + dy * dy);
                if (d < 160 && d > 0) {
                    var force = (160 - d) / 160;
                    p.vx += (dx / d) * force * 3.5;
                    p.vy += (dy / d) * force * 3.5;
                }
            });
        });

        /* ── Profile 3D tilt ── */
        if (pp) {
            pp.addEventListener('mousemove', function (e) {
                var r = pp.getBoundingClientRect();
                var ry = ((e.clientX - r.left) / r.width - 0.5) * 10;
                var rx = (0.5 - (e.clientY - r.top) / r.height) * 10;
                pp.style.transform = 'perspective(1200px) rotateX(' + rx + 'deg) rotateY(' + ry + 'deg)';
            });
            pp.addEventListener('mouseleave', function () {
                pp.style.transform = 'perspective(1200px) rotateX(0) rotateY(0)';
                pp.style.transition = 'transform .5s ease';
            });
            pp.addEventListener('mouseenter', function () {
                pp.style.transition = 'none';
            });
        }

        /* ── Scroll reveal ── */
        var revs = document.querySelectorAll('.reveal');
        var revObs = new IntersectionObserver(function (entries) {
            entries.forEach(function (e) {
                if (e.isIntersecting) { e.target.classList.add('in'); revObs.unobserve(e.target); }
            });
        }, { threshold: 0.1 });
        revs.forEach(function (el) { revObs.observe(el); });

        /* ── Progress bars ── */
        var bars = document.querySelectorAll('.pfill');
        var barObs = new IntersectionObserver(function (entries) {
            entries.forEach(function (e) {
                if (!e.isIntersecting) return;
                e.target.style.width = (e.target.dataset.p || 0) + '%';
                barObs.unobserve(e.target);
            });
        }, { threshold: 0.4 });
        bars.forEach(function (b) { barObs.observe(b); });

        /* ── Active nav highlight ── */
        var navAs = document.querySelectorAll('.nav-links a');
        var sects = document.querySelectorAll('section[id], header[id]');
        function syncNav() {
            var cur = '';
            sects.forEach(function (s) { if (window.scrollY >= s.offsetTop - 140) cur = s.id; });
            navAs.forEach(function (a) {
                a.classList.remove('active');
                if (a.getAttribute('href') === '#' + cur) a.classList.add('active');
            });
        }
        syncNav();
        window.addEventListener('scroll', syncNav, { passive: true });

        /* ── Categorized Project Tabs ── */
        var tabBtns = document.querySelectorAll('.tab-btn');
        var tabContents = document.querySelectorAll('.tab-content');

        tabBtns.forEach(function (btn) {
            btn.addEventListener('click', function () {
                tabBtns.forEach(function (b) { b.classList.remove('active'); });
                tabContents.forEach(function (c) { c.classList.remove('active'); });

                btn.classList.add('active');
                var tabId = btn.getAttribute('data-tab');
                var targetContent = document.getElementById(tabId);
                if (targetContent) {
                    targetContent.classList.add('active');
                }
            });
        });

        /* ── Lightbox Image Zoom ── */
        var lb = document.getElementById('lightbox');
        var lbImg = document.getElementById('lightboxImg');
        var lbCap = document.getElementById('lightboxCaption');
        var lbClose = document.getElementById('lightboxClose');
        var zooms = document.querySelectorAll('.zoomable');

        zooms.forEach(function (item) {
            item.addEventListener('click', function () {
                var src = item.getAttribute('data-src');
                var title = item.getAttribute('title') || '';
                if (src) {
                    lbImg.src = src;
                    lbCap.textContent = title;
                    lb.classList.add('active');
                }
            });
        });

        if (lbClose) {
            lbClose.addEventListener('click', function () {
                lb.classList.remove('active');
            });
        }
        if (lb) {
            lb.addEventListener('click', function (e) {
                if (e.target === lb || e.target === lbClose) {
                    lb.classList.remove('active');
                }
            });
        }
    </script>

    <!-- Lightbox Zoom Overlay -->
    <div class="lightbox" id="lightbox">
        <div class="lightbox-close" id="lightboxClose">&times;</div>
        <div class="lightbox-content">
            <img id="lightboxImg" src="" alt="Zoomed view" />
            <div class="lightbox-caption" id="lightboxCaption"></div>
        </div>
    </div>

</body>

</html>